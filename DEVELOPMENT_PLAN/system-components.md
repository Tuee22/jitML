# System Components

**Status**: Authoritative source
**Supersedes**: N/A
**Referenced by**: [README.md](README.md),
[development_plan_standards.md](development_plan_standards.md),
[00-overview.md](00-overview.md),
[legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md),
[phase-0-planning-documentation.md](phase-0-planning-documentation.md),
[phase-1-haskell-cli-surface.md](phase-1-haskell-cli-surface.md),
[phase-2-bootstrap-reconciler-and-jit-cache.md](phase-2-bootstrap-reconciler-and-jit-cache.md),
[phase-3-cluster-substrate-and-routing.md](phase-3-cluster-substrate-and-routing.md),
[phase-4-stateful-platform-services.md](phase-4-stateful-platform-services.md),
[phase-5-jitml-service-daemon.md](phase-5-jitml-service-daemon.md),
[phase-6-numerical-core.md](phase-6-numerical-core.md),
[phase-7-jit-codegen-and-substrates.md](phase-7-jit-codegen-and-substrates.md),
[phase-8-supervised-and-rl-framework.md](phase-8-supervised-and-rl-framework.md),
[phase-9-rl-catalog-alphazero-and-tuning.md](phase-9-rl-catalog-alphazero-and-tuning.md),
[phase-10-checkpointing-and-inference.md](phase-10-checkpointing-and-inference.md),
[phase-11-purescript-frontend-and-demo.md](phase-11-purescript-frontend-and-demo.md),
[phase-12-test-stanzas-and-cross-cluster.md](phase-12-test-stanzas-and-cross-cluster.md),
[phase-13-no-caveat-model-runtime.md](phase-13-no-caveat-model-runtime.md),
[phase-14-interactive-demo-and-playwright-closure.md](phase-14-interactive-demo-and-playwright-closure.md),
[phase-15-linux-cuda-and-cluster-closure.md](phase-15-linux-cuda-and-cluster-closure.md),
[phase-16-apple-silicon-closure.md](phase-16-apple-silicon-closure.md),
[phase-17-cross-substrate-and-handoff.md](phase-17-cross-substrate-and-handoff.md),
[phase-18-no-caveat-product-handoff.md](phase-18-no-caveat-product-handoff.md),
[phase-19-product-truth-gates.md](phase-19-product-truth-gates.md),
[phase-20-de-fossilization-and-scaffold-lint.md](phase-20-de-fossilization-and-scaffold-lint.md),
[phase-21-type-state-dsl-and-inference-eligibility.md](phase-21-type-state-dsl-and-inference-eligibility.md),
[phase-22-canonical-matrix-and-dataset-integrity.md](phase-22-canonical-matrix-and-dataset-integrity.md),
[phase-23-general-differentiable-layer-engine.md](phase-23-general-differentiable-layer-engine.md),
[phase-24-real-supervised-architectures.md](phase-24-real-supervised-architectures.md),
[phase-25-real-rl-algorithms-and-environments.md](phase-25-real-rl-algorithms-and-environments.md),
[phase-26-alphazero-real-self-play.md](phase-26-alphazero-real-self-play.md),
[phase-27-demo-all-model-rendering.md](phase-27-demo-all-model-rendering.md),
[phase-28-per-model-integration-and-e2e.md](phase-28-per-model-integration-and-e2e.md),
[phase-29-linux-cuda-product-lane.md](phase-29-linux-cuda-product-lane.md),
[phase-30-apple-silicon-product-lane.md](phase-30-apple-silicon-product-lane.md),
[phase-31-no-caveat-product-aggregation.md](phase-31-no-caveat-product-aggregation.md),
[../README.md](../README.md)
**Generated sections**: none

> **Purpose**: Authoritative target component inventory for the jitML Haskell CLI, the
> three substrates, the bootstrap reconciler, the cluster substrate, the stateful
> platform services, the `jitml service` daemon, the numerical core, the JIT codegen
> drivers, the SL/RL/AlphaZero/tuning surfaces, the checkpoint format, the PureScript
> frontend, and the Cabal test stanzas.

The inventory documents the authoritative target end state and the present
checked-in implementation. **Current status (2026-07-01):** product closure is
reopened. Phases `0`–`18` remain historical evidence for their owned surfaces,
but current no-caveat completion is blocked on Phases `19`–`31`: product-truth
gates and registry, de-fossilization and scaffold lint, type-state inference
eligibility, canonical matrix/data integrity, a general differentiable layer
engine, real supervised architectures, real RL algorithms/environments,
AlphaZero real self-play per game, all-row demo rendering, per-row
integration/e2e coverage, `linux-cuda` row validation, `apple-silicon` row
validation, and final `linux-cpu` aggregation. Component rows that contribute to
the product model surface are incomplete until their Phase `19`–`31` evidence
exists.

Historical closure evidence remains in the phase files and attestation files as
dated record. The 2026-06-28 `linux-cuda` HA attestation, the 2026-06-29
`apple-silicon` HA attestation, and the 2026-06-29 `linux-cpu` aggregation are
the HA evidence set that Sprint `18.6` consumes alongside the 2026-06-30
typed-failure/docs-governance re-aggregation.

Per-row component cells below describe current worktree state. A component row
is `✅ Done` only when every current Exit-Definition obligation that the row
contributes to is met in the worktree.

## Substrates

| Substrate | Identifier | Codegen | Container Shape | Daemon Topology | Status | Owning Phase |
|-----------|------------|---------|-----------------|-----------------|--------|--------------|
| Apple Silicon | `apple-silicon` | Haskell renders canonical MSL plus launch metadata, persists `./.build/jit/apple-silicon/<hash>.metal.json`, and calls a fixed host Metal bridge that invokes `MTLDevice.makeLibrary(source:options:)` with fast math disabled before dispatching on the host GPU. Core cache misses require `apple.metal-runtime` + `apple.metal-bridge`, not Tart, SwiftPM, full Xcode, the offline `metal` compiler, or keychain state. MLP forward/backward/batch kernels use the same fixed bridge ABI. | partial — cluster services in Kind; second `jitml service` runs host-native (Metal cannot be containerized) | two instances of one binary, distinguished by Dhall: `Cluster + ForwardToHost` (in-pod orchestrator) + `Host + SelfInference` (host-native execution). Sprint `5.11` extends host residency beyond inference to Training/RL/Tune starts through `training.host-command.apple-silicon`, `rl.host-command.apple-silicon`, and `tune.host-command.apple-silicon`; host daemons run with `httpListener = None` and consume work through Pulsar/MinIO only. | ✅ Done for the fixed-bridge substrate/runtime surface and the Phase `16` HA fixed-budget all-model lane; Sprint `16.14` revalidated it on Apple M1 Max / macOS 26.5 / Metal 4 with a live **131-step** HA rollout at edge `:9090`, host Metal daemon subscriptions, `bootstrap/apple-silicon.sh test` **8 / 8**, direct inference `HTTP 200`, eight seeded demo checkpoints, and Playwright **15 / 15**. The Apple stage-0 build path selects Homebrew `llvm@19` when PATH lacks a GHC-compatible `opt`/`llc`. | [Phase 5](phase-5-jitml-service-daemon.md) / [Phase 7](phase-7-jit-codegen-and-substrates.md) / [Phase 16](phase-16-apple-silicon-closure.md) |
| Linux CPU | `linux-cpu` | generated libdnnl-linked oneDNN C++ source under `./.build/jit-src/linux-cpu/<hash>/`, plus local identity/reorder, reduction, dense/matmul, convolution, normalization, attention/matmul, and embedding/reorder compile/load/run through `JitML.Engines.Loader` and `JitML.Engines.Local` with exported `jitml_kernel_family_name` and `jitml_kernel_output_count` verification; local cache keys include `artifact-abi=<os>-<arch>` and `reduction-block=256` in the toolchain fingerprint; `JitML.Engines.CpuFeatures` probes AVX2/AVX-512 through typed subprocesses for the oneDNN micro-kernel knob; `JitML.Engines.OneDnnRuntime` probes `pkg-config`, readable oneDNN headers, and dynamic-linker `libdnnl` visibility through typed subprocesses; `jitml service` routes `linux-cpu` + `SelfInference` requests through `loadInferenceCheckpointWithWeights` and `runLinuxCpuWeightedCheckpointInference` after MinIO latest-pointer/manifest/weight loading | fully containerized target: `jitml:local` | one daemon: `Cluster + SelfInference`; current code renders configs and summaries, validates same-host oneDNN primitive kernel paths, and wires daemon inference dispatch to the Linux CPU weighted FFI runner | ✅ Done for Phase 7.3 Linux CPU runtime; later model tensor payload growth can reuse the same ABI | [Phase 7](phase-7-jit-codegen-and-substrates.md) |
| Linux CUDA | `linux-cuda` | generated CUDA C source under `./.build/jit-src/linux-cuda/<hash>/`; host SplitMix64 RNG surface, generated no-curand metadata, deterministic reduction source with no `atomicAdd`, generated host-callable `jitml_kernel(float*, const float*, size_t)` wrapper that allocates device buffers / launches / synchronizes / copies output back, generated family/output-count metadata symbols, guarded local CUDA loader in `JitML.Engines.CudaLocal`, `LocalCudaEngine` `HasEngine` dispatch, host reduction partial-count/finalization helpers, and typed CUDA runtime probes for `nvcc`, `nvidia-smi`, and dynamic-linker visibility; Phase `15` adds live per-family weighted CUDA bodies plus nvcc forward/backward MLP kernels validated on the Linux/NVIDIA host | fully containerized target: `jitml:local` (CUDA activates at runtime when scheduled to `runtimeClassName: nvidia`) | one daemon topology: three `Cluster + SelfInference` Engine replicas in the HA local profile, scoped with `jitml.compute-scope=service`; daemon-spawned Training/RL/Tune/Inference Jobs use `jitml.compute-scope=workload`, so service anti-affinity and workload anti-affinity each enforce at most one numerical worker of that scope per node without deadlocking each other. Live `RuntimeClass/nvidia` GPU visibility, CUDA compile/load/run, and live inference dispatch are owned by Phase `15` | ✅ Done for the Phase `7` CUDA runtime surface and the Phase `15` HA fixed-budget all-model lane; Sprint `15.22` revalidated the lane on the RTX 5090 host with HA rollout 130 steps, staged canonical datasets, `jitml test all --linux-cuda` 8/8, `jitml-backends` 20/20, seeded demo checkpoints, Playwright 15/15, `docs check`, and `check-code`. | [Phase 7](phase-7-jit-codegen-and-substrates.md) / [Phase 15](phase-15-linux-cuda-and-cluster-closure.md) |

A fourth substrate `linux-opencl` (Intel GPU) is admitted as a future extension and
is not in the current support matrix.

## Haskell CLI Surface

| Surface | Command | Purpose | Status | Owning Sprint |
|---------|---------|---------|--------|---------------|
| Bootstrap | `jitml bootstrap --apple-silicon\|--linux-cpu\|--linux-cuda` | Plan/Apply substrate bootstrap surface; non-dry-run apply materializes Kind/chart/Dhall/publication files locally and then runs `JitML.Bootstrap.liveExecutePhasedRollout` through typed `kind` / Helm / Docker build and explicit Kind image-load / repo-owned manifest apply / readiness / Pulsar-topic subprocesses with no process-environment gate, writing the leased-port publication with Helm-status-derived component health. Repo-owned local images are rebuilt during bootstrap instead of being skipped when a stale `jitml:local` tag already exists; third-party warm-cache image loads remain filtered separately, and the Linux compose bootstrap forwards `JITML_BOOTSTRAP_SKIP_IMAGE_BUILD` only when the caller has already built the image being attested. Sprint `3.6` materializes the HA Kind shape (one control-plane plus three workers), applies resource caps/mount prep to every Kind node, and keeps one localhost Envoy socket. Sprint `2.13` (✅ Done on its retained surface) adds an authenticated host pre-pull of the `docker.io/*` third-party chart images before `kind load` (reading, never writing, the host `docker login`) so a cold host no longer falls back to anonymous in-cluster Docker Hub pulls and the 429 rate limit; live-proven (no 429 on the host pull), and on an overlay2 docker store the pre-pull + `kind load` closes the in-cluster 429 directly. The containerd-image-store in-cluster load/auth closure (colima `kind load` ↔ `ctr import` incompatibility) is closed by jitML's own Sprint `2.14` in-cluster `imagePullSecret` containerd-registry-auth mechanism — an owned, self-contained path. | ✅ Done for current HA materialization and the `linux-cuda` HA rollout; Apple HA rollout revalidation is owned by Phase `16`. | Sprints 2.1–2.7, 3.5, 3.6 / Sprint 12.12 / Sprint 2.13 |
| Service daemon | `jitml service` | Renders daemon lifecycle, BootConfig/LiveConfig, endpoint, logging, retry, at-least-once helper surfaces, BootConfig-derived client acquisition settings, daemon-side Apple Metal fixed-bridge/runtime acquisition status, BootConfig-derived daemon Pulsar subscription plans, per-topic startup subscription-acquisition status, combined `DaemonServiceClient` capability interpreter, read-only non-Pulsar client probe status, typed mutating workload-effect runner plus byte-faithful parsed dispatcher and Training/RL/Tune/Inference command-envelope dispatcher, bounded acquired-subscription consumer batch exposed by `--consume-once <n>`, normal-service held-open WebSocket consumer workers with one shared process-lifetime `HandlerRouter`, post-dispatch WebSocket ack and negative-ack surfaces, and LiveConfig-derived dedup cache size/TTL; starts the in-binary HTTP listener, and the local Helm chart carries the current typed Dhall ConfigMap surface. Worker Jobs mount typed `RunConfig.dhall`; an existing malformed mount is fatal (`InvalidConfig`) instead of falling back to env/default values, while missing mounts retain developer-local fallback behavior. Sprint `5.16` makes the Engine/numerical compute role schedule at most one ML worker of each compute scope per Kubernetes node while Coordinator, Webapp, observability, and platform services may scale independently. Linux Engine pods use `jitml.compute=true` plus `jitml.compute-scope=service`; daemon-spawned Linux workload Jobs use `jitml.compute=true` plus `jitml.compute-scope=workload`. Each scope has its own anti-affinity/topology-spread selector, so HA service replicas and live workload Jobs both spread across worker nodes without blocking each other. Apple clustered pods are non-compute forwarders. | ✅ Done; Sprint `5.17` added fail-closed mounted RunConfig decoding and passed `jitml-unit` 239/239, `jitml-daemon-lifecycle` 32/32, live `jitml-integration --linux-cpu` 77/77, and `check-code: ok`. | Sprints 5.1, 5.4, 5.5, 5.6, 5.10, 5.11, 5.16, 5.17 |
| Cluster lifecycle | `jitml cluster up`, `jitml cluster down`, `jitml cluster status`, `jitml cluster reset` | `up` is a real lower-level Kind/Helm reconciler matching the generated CLI docs: it materializes the selected substrate files, runs the typed dependency build / Kind create-export / Docker build / explicit Kind image-load / Helm-local apply / readiness / Pulsar-topic phases, and writes measured publication health only after live readiness with `evidence: live-readiness`; `status` fails closed on missing, corrupt, or default/local publications without that evidence; `down` deletes the publication-named Kind cluster through a typed idempotent subprocess, preserves `./.build` and `./.data`, and marks preserved publication components `stopped`; `reset` remains the guarded destructive local-state summary | ✅ Done; Sprint `3.7` live-validated `cluster up --substrate linux-cpu`, `cluster status`, and `jitml-integration --linux-cpu` 77/77 including 19/19 `Live` cases plus missing/corrupt/no-evidence status regressions | Sprint 3.7 |
| Train | `jitml train` | Sprint 8.12 fail-closed: `runDeviceMnistTraining` **requires** a live publication plus a staged canonical dataset, resolves the experiment Dhall through `JitML.SL.Canonicals.loadCanonicalProblemExperiment`, fetches MinIO blobs, gunzips/IDX-parses or archive-parses the staged artifacts, decodes examples through `JitML.SL.Classifier`, `JitML.SL.TinyImageNet`, or `JitML.SL.Regression`, resolves the row's `JitML.SL.Architecture.ArchitectureSpec`, and trains through the resolved substrate's JIT `MlpDevice` (`trainArchitectureWithDevice` or the regression MSE trainer), publishing the device-measured `training.event.<mode>`; an unmet prerequisite → `TrainingPrerequisiteUnmet` (exit 2) with nothing printed/published. Current product closure requires Phase `22` read-time SHA verification, Phase `24` literal documented SL architecture parity plus weight-update/convergence evidence, Phase `21` type-state inference eligibility, and Phase `28` per-row integration/e2e evidence. `JitML.Proto.Training` text/proto3 byte codecs cover the command/event envelopes. | 🔄 Product incomplete; prior train-step/runtime evidence is historical and the no-caveat SL product surface is owned by Phases `21`, `22`, `24`, `28`, `29`, `30`, and `31`. | Sprint 8.2 / 8.10 / 8.12 / 8.14 / 15.4 / Phase 13 / Phases 21-31 |
| Eval | `jitml eval` | Current command accepts a checkpoint selector and prints a deterministic summary; deeper checkpoint-backed evaluation is not an Exit-Definition-owned surface beyond the inference read path | ✅ Done for current CLI surface | Sprint 8.2 |
| Tune | `jitml tune` | Decodes `experiments/mnist-tune.dhall` through `JitML.Tune.Catalog`, applies CLI overrides to the resolved experiment before validation and artifact writing, renders the actual sampler/scheduler/pruner axes, computes real measured `valAcc` trial objectives (not LCG values), writes the best local trial's trained weights as a `.jmw1` checkpoint, emits a `tune-trials` artifact, and exposes `trialObjectiveResultWithDevice` for substrate-backed trial training through the selected JIT `MlpDevice`; daemon-dispatched live work reads sampler/scheduler/pruner/trial budget from `TuneRunConfig`, persists trial transcripts to MinIO, promotes measured trial weights into `jitml-checkpoints`, and publishes `TuneTrialStarted` / `TuneTrialFinished` / `TuneSweepDone`. Linux lanes execute this through worker Jobs; Sprint `5.11` routes Apple Metal-backed trials to the host workload command path. `JitML.Proto.Tune` text/proto3-compatible byte codecs cover the current envelopes | ✅ Done; Sprint `9.16` proves CLI overrides drive local artifacts and daemon workers use mounted `TuneRunConfig` axes, with `jitml-hyperparameter --linux-cpu` 17/17 and live `jitml-integration --linux-cpu` 77/77 | Sprint 9.16 |
| RL lifecycle | `jitml rl train`, `jitml rl eval`, `jitml rl rollout`, `jitml rl alphazero self-play` | Commands render algorithm/checkpoint/trajectory summaries: `jitml rl train` reads typed `RunConfig`, routes MLP-backed trainers through the selected substrate `MlpDevice`, writes `.jmw1` checkpoints where trainer weights exist, emits trajectory artifacts, and publishes `rl.event.<mode>` envelopes. Current product closure requires Phase `25` to prove every documented algorithm/environment row dispatches to the requested environment instead of a hardcoded one, removes deterministic product stand-ins, records weight/policy update evidence, and classifies non-neural or optional rows explicitly. Phase `21` owns type-state inference eligibility; Phase `28` owns per-row integration/e2e coverage; Phases `29` and `30` own accelerator lane proof. | 🔄 Product incomplete; Sprint `8.15` typed-error work remains historical evidence, but real RL row completion is owned by Phases `21`, `25`, `28`, `29`, `30`, and `31`. | Sprint 8.5 / 8.11 / 8.14 / 8.15 / 9.9 / 9.10 / 9.11 / 9.12 / 9.14 / 12.11 / 15.5 / 15.6 / 15.8 / 16.10 / Phase 14 / Phases 21-31 |
| CLI Dhall override surface | `jitml train --substrate <s> --seed <n>`, `jitml rl train --substrate <s> --seed <n>`, `jitml tune --sampler <s> --scheduler <s> --pruner <p> --trials <n> --parallelism <n>` | Parser and pure resolver surface: `JitML.Experiment.Overrides.applyOverrides` substitutes CLI values into the parsed experiment Dhall before validation; overrides act on the named axis only and never replace the whole record per [../README.md → Why this exists](../README.md#why-this-exists) pillar 2 and [../README.md → Hyperparameter tuning, first-class](../README.md#hyperparameter-tuning-first-class). Substrate-name parsing rejects bare `cpu` / `cuda` aliases; canonical identifiers `apple-silicon` / `linux-cpu` / `linux-cuda` only. Sprint `1.17` extends the same typed-parse discipline to remaining numeric CLI values such as `service --consume-once`, `rl rollout --seed`, and AlphaZero self-play budgets. Sprint `9.16` applies parsed tune overrides to actual local tune artifacts and worker selection before closure. | ✅ Done for parser/validation and tuning execution fidelity | Sprint 1.12 / Sprint 1.17 / Sprint 9.16 |
| Inference | `jitml inference run` | Runs deterministic local inference summaries and, when a live publication exists, reads checkpoint manifests / `.jmw1` weight blobs from live MinIO before dispatching to the substrate-visible weighted runner; `JitML.Proto.Inference` round-trips `InferenceRequest` / `InferenceResult` through proto3-compatible bytes, and daemon dispatch routes Linux CPU `SelfInference` through `runLinuxCpuCheckpointInference`. The shared checkpoint loaders now call `requireInferenceEligibleCheckpoint` before invoking a runner, so partial manifests remain inspectable but are rejected for inference. | ✅ Done; endpoint/browser paths expose the `InferenceEligibleCheckpoint` boundary, partial manifests are rejected, and Phase `13` proved all-model inference from completed fixed-budget artifacts on `linux-cpu` | Sprint 10.4 / Sprint 15.12 / Sprint 10.10 / Phase 13 |
| Test runner | `jitml test all` / `jitml test <stanza>` | `--dry-run` renders the aggregate plan; non-dry-run invokes Cabal over the explicit eight test-only stanza names or a selected test stanza through typed `Subprocess` (one Cabal subprocess per stanza under a substrate selector, so live tests do not contend over one cluster/device), parses the `cabal.project` report-card knob block, then emits a report card naming the actual stanzas that ran. `jitml test all --live` appends measured SL / RL / AlphaZero / tune / cache / daemon fields and renders unreachable live sources as `unavailable`; cache telemetry reads `/metrics` and daemon health reads `/healthz` through the published edge routes when a live publication exists. Sprint 12.11 adds `JitML.Test.WorkflowMatrix`, a DRY fail-closed live integration matrix for every reopened workflow × substrate; local e2e asserts structural matrix coverage, and `jitml-integration` Live executes the current-substrate matrix cells. Sprint `12.12` adds failed-Job observation with status/pod-log rendering, bounded host-command polling, and placement assertions: Linux Training/RL/Tune still expect Jobs, while Apple Metal-backed Training/RL/Tune expect host commands and no workload Jobs. Sprint `12.15` adds pure all-model cell enumeration and a checkpoint-loader infer-before-complete negative. Lint/style/code-quality commands are separate. | ✅ Done; `jitml test all --live --linux-cpu` passed 8/8 with every report-card measurement populated, all-model trained-artifact browser evidence, and infer-before-complete negatives | Sprint 12.9 / Sprint 12.11 / Sprint 12.12 / Sprint 12.15 / Sprint 17.2 |
| Lint stack | `jitml lint files\|docs\|proto\|chart\|haskell\|purescript\|all` | In-repo hygiene, config, forbidden-path, generated-doc, chart-shape, forbidden-primitive, static-JIT-artifact, Fourmolu, HLint, and `cabal format` checks are implemented and execute only inside `jitml:local`; host lint execution fails before linting rather than discovering or bootstrapping style tools | ✅ Done | Sprint 1.4 |
| Docs generation | `jitml docs check` / `jitml docs generate` | Paired generated-section check and write per the `GeneratedSectionRule` registry | ✅ Done | Sprint 1.3 |
| Command introspection | `jitml commands [--tree\|--json]` | Flat list, tree rendering, or JSON command schema from the `CommandSpec` registry | ✅ Done | Sprint 1.2 |
| Focused help | `jitml help <subcommand>` | Equivalent to `<subcommand> --help`; same renderer | ✅ Done | Sprint 1.2 |
| Code quality gate | `jitml check-code` | Delegates to `jitml lint all` and adds the warning-clean `cabal build all --ghc-options=-Werror` gate; `docker/Dockerfile` runs it during `jitml:local` image construction, and runtime use is supported only inside `jitml:local` | ✅ Done | Sprint 1.4 |
| Build | `jitml build` | Build-plan surface for substrate JIT artifacts: `--dry-run` renders selected tuning metadata plus generated-source and compile plans; non-dry-run routes the selected artifact through `JitML.Engines.Loader`. The Docker image build owns installing the `jitml` binary inside the container. | ✅ Done | Sprints 2.4, 7.1 |
| Prerequisite doctor | `jitml doctor [--scope toolchain\|container\|cluster] [--remediate]` | In-process prerequisite registry reconciliation and typed remediation apply/postcondition validation | ✅ Done | Sprint 2.2 |
| Internal prerequisite listing | `jitml internal list-prereqs` | Prints the typed prerequisite registry from the current Haskell process | ✅ Done | Sprint 1.7 |
| Internal substrate materialization | `jitml internal materialize-substrate` | Validates the supported substrate identifier and renders Kind config, chart templates, publication metadata, and per-substrate Dhall | ✅ Done | Sprint 2.1 / Sprint 3.1 |
| Internal VM lifecycle (Apple) | retired Tart VM surface | Deleted from the supported Apple path. The target Apple path has no VM lifecycle command or runtime prerequisite; fixed-bridge diagnostics flow through `doctor` / `internal list-prereqs` and `jitml internal install-metal-bridge`. | ✅ Done; Sprint `1.15` removed the command surface, Sprint `2.12` removed core prerequisite/bootstrap cleanup, Sprint `5.10` removed daemon build-VM acquire/config, and Sprint `7.11` removed the generated Swift/Tart cache-miss code path. Historical Tart evidence remains in dated phase notes only. | Sprints 1.15 / 2.12 / 5.10 / 7.11 |
| Internal cache inspection | `jitml internal cache stat\|list\|evict` | Reads the local `.build/jit` manifest and artifact tree. `stat` reports real manifest-entry/file/byte counts, `list` renders manifest entries plus unowned files, and `evict <hash>` deletes matching artifact files and removes matching manifest entries. | ✅ Done for local cache inspection and eviction | Sprint 7.1 / Sprint 17.2 |
| Internal reflected Dhall schema | `jitml internal dhall-schema [--config <surface>] [--catalog numerics\|rl\|all]` | Prints the binary's own reflected Dhall schema — daemon config surfaces (`BootConfig`/`LiveConfig`/`RunConfig`) off the live `FromDhall` decoders via `Dhall.expected` (Sprint 5.12), and the numerics/RL catalog `.dhall` leaves rendered from the Haskell catalogs (`JitML.Service.CatalogSchema`, Phase 5, 2026-06-23). Parity-tested in `jitml-unit` against the checked-in files | ✅ Done | Sprint 5.12 / Phase 5 |
| Internal GC | `jitml internal gc <experiment-hash>` | Walks the local checkpoint store when no publication is present; with a live publication it lists MinIO manifests, builds and executes the retention plan through `MinIOSubprocess`, reaps blobs/manifests, publishes `gc.event.<substrate>` envelopes for successful reaps, prints kept/reaped counts, and exits `3` on no-op | ✅ Done; live MinIO reaping and `GcReapedEvent` publication are validated in `jitml-integration` | Sprint 10.3 / Sprint 15.7 |
| Webapp workload (`jitml-demo`) | `chart/local/jitml-demo`; runs `jitml service --config /etc/jitml/BootConfig.dhall` with `activeRole = Webapp`. The workload serves the frontend/API route surface through `src/JitML/Web/Server.hs`. `Web.Bundle.demoRoutes` names the current route family including `/api/inference/generic`, `/api/checkpoints/compare`, `/api/runs/{runId}/command`, and `/api/ws/rl`; `loadBundleEntry` + `demoHttpRoutesWithBundle` append a `/bundle/main.js` route serving the browser-loadable Halogen bundle from `web/dist/Main/bundle.js` when present. `/api/inference`, `/api/inference/generic`, `/api/images`, `/api/checkpoints/compare`, and `/api/connect4/move` publish WorkCommands to the Engine and fail closed without live publication; the Webapp computes no ML. Plain HTTP stream GETs return `503` unless the client performs a WebSocket upgrade. `serveDemoWithBridgeEndpointWithRuntime` proxies live `/api/ws*` streams to Pulsar event topics and resolves browser command envelopes from `/api/runs/{runId}/command` into live `training.command.<substrate>`, `rl.command.<substrate>`, or `tune.command.<substrate>` publications. | ✅ Done; model-facing demo paths consume only inference-eligible artifacts and render completion/convergence/TensorBoard evidence in the `linux-cpu` product baseline | Sprint 11.5 / 11.8 / 15.13 / 17.3 / Sprint 10.6 / Sprint 11.9 / Sprint 11.10 / Phase 14 |

## Bootstrap Stage-0 Script Surface

The shell scripts are stage-0 entrypoints, not broad package reconcilers. They
fail fast with installation instructions, then delegate to the Haskell
`jitml bootstrap --<substrate>` command.

| Verb | Purpose | Status | Owning Sprint |
|------|---------|--------|---------------|
| `help` | Print supported subcommands | ✅ Done | Sprint 2.1 |
| `doctor` | Run only stage-0 host gates: Apple macOS/arm64/Xcode CLT/Homebrew; Linux Docker without `sudo`; CUDA NVIDIA runtime and compute capability | ✅ Done | Sprint 2.1 |
| `build` | Apple convenience to build `./.build/jitml`; on Darwin it exposes a GHC 9.12.4-compatible LLVM `opt`/`llc` pair by accepting PATH tools in `[13,20)` or prepending an installed Homebrew `llvm@19` ... `llvm@13` keg; Linux builds the one-service `jitml:local` Compose image | ✅ Done | Sprint 2.1 / Sprint 2.4 |
| `up` | Apple builds `./.build/jitml` and delegates to `jitml bootstrap --apple-silicon`; Linux calls the intended Compose handoff, with the actual `compose.yaml` target delivered by Sprint 2.4 | ✅ Done | Sprint 2.1 |
| `status` | Report bootstrap-side stack health from `./.build/runtime/cluster-publication.json` | ✅ Done | Sprint 2.6 |
| `test` | Thin wrapper for `jitml test all` from outside the container | ✅ Done | Sprint 2.6 |
| `down` | Tear down the cluster; preserve `./.data/` and `./.build/`; target Apple path has no VM lifecycle to stop | ✅ Done | Sprint 2.7 |
| `purge` | Cluster down, `rm -rf ./.data/`; Apple has no VM lifecycle to destroy; preserves `./.build/` (including JIT cache and the fixed bridge) | ✅ Done | Sprint 2.7 / Sprint 2.12 |
| `purge --full` | `purge` plus `rm -rf ./.build/`; Linux additionally drops the substrate image | ✅ Done | Sprint 2.7 |

## Cluster Substrate Components

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Per-substrate Kind config | `JitML.Cluster.Kind` renders one control-plane plus the HA worker count from `dhall/cluster/resources.dhall` for every substrate, while preserving one public edge socket on the control-plane. Checked-in `kind/cluster-*.yaml` fixtures are materialized from that renderer. | ✅ Done | Sprint 3.1 / Sprint 3.6 |
| Kind node labels (CUDA) | Linux CUDA worker nodes carry `jitml.node-role/compute=true` and `jitml.runtime/gpu=true` so the `nvidia` RuntimeClass binds to compute-capable workers; the control-plane carries only the substrate label. | ✅ Done | Sprint 3.1 / Sprint 3.6 |
| Kubeconfig home | `./.build/jitml.kubeconfig`; `~/.kube/config` is never touched | ✅ Done | Sprint 3.5 |
| `extraMounts` of `./.build/` | HA Kind topology bind-mounts required repo-local state consistently on every materialized Kind node that can run jitML workloads. | ✅ Done | Sprint 3.1 / Sprint 3.6 |
| `dhall/cluster/` resource profile | Typed `ClusterResources` Dhall (`dhall/cluster/Schema.dhall` + concrete values) decoded via `Dhall.inputFile` in `JitML.Cluster.Resources`, materialized to `./.build/conf/cluster/Resources.dhall`; single source of truth for the kind-node cap and the per-pod budgets/replica counts | ✅ Done | Sprint 2.8 |
| Kind-node resource cap (host safety) | Bootstrap applies typed memory/CPU caps to every materialized Kind node from the HA profile, not just the control-plane. | ✅ Done for materialization; live cap revalidation remains with Phases `15`/`16`. | Sprint 2.8 / Sprint 3.6 / Sprint 15.1 |
| `jitml-manual` StorageClass | `kubernetes.io/no-provisioner`; the only StorageClass in the chart | ✅ Done | Sprint 3.2 |
| Manual PV templates | HA PV templates cover every persistent replica in the HA storage topology: distributed MinIO, Pulsar ZooKeeper/BookKeeper state, Percona Postgres instances, and pgBackRest. | ✅ Done | Sprint 3.2 / Sprint 3.6 |
| HostPath layout | HA layout remains `./.data/<namespace>/<workload>/pv_<replica-int>/` and allocates one path per HA replica/PV. | ✅ Done | Sprint 3.2 / Sprint 3.6 |
| `GatewayClass/jitml-gateway` + `Gateway/jitml-edge` | Single localhost listener at `127.0.0.1:<edge-port>`; GatewayClass attaches `EnvoyProxy/jitml-edge` via `parametersRef` | ✅ Done | Sprint 3.3 |
| `EnvoyProxy/jitml-edge` | NodePort service, `externalTrafficPolicy: Cluster`, Gateway listener port pinned to NodePort 30090. HA keeps exactly one public localhost edge socket while backend services scale behind it. | ✅ Done | Sprint 3.3 / Sprint 3.6 / Sprint 12.11 |
| Route registry | `src/JitML/Routes.hs` — single source of truth for every HTTPRoute resource; Phase `4` routes target the actual Helm release services, send Harbor's public portal/API/registry/token paths through the chart's public `harbor` service, and send `/pulsar/ws` to the broker's embedded WebSocket service on `pulsar-broker:8080` | ✅ Done | Sprint 3.4 |
| Generated HTTPRoute manifests | `chart/templates/httproute-*.yaml`, rendered from the route registry; hand edits fail `jitml lint chart` | ✅ Done | Sprint 3.4 |
| Cluster publication | `./.build/runtime/cluster-publication.json` — `edge_port`, Pulsar URL, MinIO URL, component health; `src/JitML/Cluster/EdgePort.hs` (`leaseEdgePort`) returns the first available 127.0.0.1 port from `[9090, 9091, 9092]` via `Network.Socket.bind`; `liveExecutePhasedRollout` writes the publication with `publicationWithLeasedPort` and replaces default component status with measured Helm release status | ✅ Done; 2026-05-23 live Linux CPU bootstrap wrote ready component health on edge port `9091`, and live teardown preserved the same publication with every component marked `stopped` | Sprint 3.5 |
| Phased deploy | Order is Harbor prerequisites first (retry-hardened MinIO bucket readiness, Percona `harbor-pg` readiness, schema grant) → Harbor → local image build/load into Kind → final HA services; code renders the ordered plan, materializes local inputs, emits typed image subprocesses, installs external Helm dependencies from generated `.tgz` archives with typed per-release values, installs jitML-owned charts, and stops at the first failed subprocess. | ✅ Done for HA materialization; live HA rollout revalidation is owned by Phases `15`/`16`. | Sprint 3.5 / Sprint 3.6 / Sprint 4.10 |
| Helm dependency build | `src/JitML/Cluster/Helm.hs` renders the typed dependency-build step before live apply; live execution skips the build when every expected `.tgz` already exists under `chart/charts/`, otherwise it invokes `helm dependency build chart`; `jitml lint chart` treats `chart/charts/` as Helm's generated archive cache and only decodes YAML manifests as text | ✅ Done; canonical command rendering, idempotent archive detection, archive-cache-safe chart lint, and live bootstrap execution are validated | Sprint 3.5 |

## Stateful Platform Services

| Component | Helm Subchart | Routing | Status | Owning Sprint |
|-----------|---------------|---------|--------|---------------|
| Harbor (image registry) | `harbor` subchart values + typed `helm install` plan; `chart/values/harbor.yaml` points the registry storage backend at MinIO bucket `harbor-registry` and the database at `harbor-pg-pgbouncer.platform.svc`; `JitML.Service.HarborSubprocess` provides explicit Docker/curl-backed `HasHarbor` settings and commands | `/harbor` (portal), `/harbor/api` (API), `/v2` (Docker registry), `/service` (token service), all through the chart's public `harbor` nginx service | ✅ Done; live Harbor rollout readiness, external Postgres/S3 wiring, registry-API validation, and Docker-backed login/push/pull/list/artifact-existence through `127.0.0.1:9091` are validated | Sprint 4.1 |
| Percona PG operator | `pg-operator` subchart values plus local PV templates and `JitML.Cluster.PostgresRegistry` `PerconaPGCluster` rendering | (internal — packaged services that require Postgres) | ✅ Done; live `harbor-pg` reconciliation, database readiness, schema grant, and Harbor startup against `harbor-pg-pgbouncer.platform.svc` are validated | Sprint 4.2 |
| Helm values ownership | `chart/templates/` is manifest-only; subchart values belong under `chart/values.yaml` unless a typed Helm invocation passes a separate values file; `jitml lint chart` rejects values files under `chart/templates/` | ✅ Done | Sprint 4.3 |
| MinIO (object store) | Distributed MinIO uses 4 replicas and the documented bucket set projected from `JitML.Storage.Buckets`; direct and umbrella values carry the HA topology. `JitML.Cluster.Readiness.minioBucketReadinessSubprocess` checks every typed bucket through the in-pod Bitnami `mc` client with bounded retries; `JitML.Service.MinIOSubprocess` provides the subprocess-backed HTTP S3 `HasMinIO` instance. | `/minio/console`, `/minio/s3` | ✅ Done for HA values/materialization; live HA revalidation remains in Phases `15`/`16`. | Sprint 4.3 / Sprint 4.10 |
| Apache Pulsar | HA topology uses 3 ZooKeeper, 3 BookKeeper, 3 Broker, and 3 Proxy replicas with the existing topic-command renderer; `broker.configData.webSocketServiceEnabled: "true"` enables the broker-embedded WebSocket service; topic creation remains the typed substrate-scoped family plus Apple host-command topics. | `/pulsar/admin`, `/pulsar/ws` (`pulsar-broker:8080`, rewrite `/ws`) | ✅ Done for HA values/materialization; live HA revalidation remains in Phases `15`/`16`. | Sprint 4.4 / Sprint 4.10 / Sprint 5.11 / Sprint 12.12 |
| Envoy Gateway controller | `gateway-helm` | (controller — dispatches the GatewayClass) | ✅ Done; live Linux CPU validation programmed the Gateway and served `/api` through the single localhost listener | Sprint 3.3 |
| kube-prometheus-stack | `kube-prometheus-stack` values plus Grafana/Prometheus renderers | `/grafana`, `/prometheus` | ✅ Done; live Prometheus + Grafana rollout readiness is validated, Grafana serves all seven generated jitML dashboards, and Prometheus reports `jitml-service` `/metrics` as an `up` target | Sprint 4.5 |
| TensorBoard | TensorBoard scalar-event codec (`JitML.Proto.TensorBoard`), TFRecord/shard writer (`JitML.Observability.TensorBoard`), checkpoint sidecar dispatcher (`JitML.Observability.TbSidecar`), and local chart; `chart/local/tensorboard` runs native `python:3.11-slim` with pinned TensorBoard and a Bitnami MinIO-client sidecar mirroring `jitml-tensorboard` into `/tensorboard/logs` | `/tensorboard` | ✅ Done; live Linux CPU validation confirms TensorBoard serves through Envoy, reads a mirrored MinIO TFRecord shard via the scalars API, live `dispatchCheckpointDone` writes the CBOR sidecar through routed MinIO, and a Haskell-written scalar shard reaches TensorBoard through routed `JitML.Service.MinIOSubprocess` | Sprint 4.6 |
| `jitml-service` Deployment | HA role split allows Coordinator/Webapp/noncompute surfaces to scale, but the Engine/numerical ML compute role schedules at most one compute worker per Kubernetes node. Linux service deployments render three Engine replicas pinned to compute workers with required hostname anti-affinity and hard topology spread; Apple clustered service renders one non-compute forwarder. | ✅ Done for HA renderer/chart/cardinality tests; live HA revalidation remains in Phases `15`/`16`. | Sprint 5.6 / Sprint 5.16 |
| `jitml-demo` Deployment | `chart/local/jitml-demo/templates/deployment.yaml`; runs the one-binary Webapp role with typed Dhall config (`activeRole = Webapp`) | `/`, `/api`, `/api/inference`, `/api/inference/generic`, `/api/images`, `/api/checkpoints/compare`, `/api/connect4/move`, `/api/ws`, `/api/ws/training`, `/api/ws/tune`, `/api/ws/rl` route family, compiled bundle serving, and live WebSocket proxy. Current product closure requires Phase `27` to render every product row from real inference-eligible artifacts and Phase `28` to prove every row through e2e coverage. | 🔄 Product incomplete; deployment/readiness/proxy mechanics are historical evidence, while all-row artifact-backed demo proof is owned by Phases `27`, `28`, `29`, `30`, and `31`. | Sprint 11.5 / Sprint 11.8 / Sprint 11.10 / Sprint 15.13 / Sprint 17.3 / Phase 14 / Phases 27-31 |
| HA resource budgets + replica topology | HA topology keeps resource budgets explicit while restoring HA replica counts for platform services: MinIO 4, Pulsar 3, service Postgres 3, and `jitml-service` Engine replicas 3 for Linux substrates. The MinIO/Pulsar readiness retries remain typed Haskell with `RetryPolicy`. | ✅ Done | Sprint 4.8 / Sprint 4.10 |
| NVIDIA RuntimeClass (CUDA) | `chart/templates/runtimeclass-nvidia.yaml`; `JitML.Cluster.Kind` renders the Linux CUDA workers' containerd `nvidia` runtime handler, repo-owned NVIDIA runtime config, read-only `/run/nvidia/driver` host driver-root mount, and toolkit support mounts; daemon-rendered `linux-cuda` service pods and worker Jobs request `runtimeClassName: nvidia` and NVIDIA visibility / driver-capability env vars | (RuntimeClass — selected by the Linux CUDA Deployment podSpec and CUDA worker Jobs) | ✅ Done; 2026-06-11 live Linux CUDA validation on an RTX 5090 confirms the runtime path, and HA renderer tests now assert the CUDA worker placement shape. | Sprint 4.7 / Sprint 15.18 |

## MinIO Bucket Layout

| Bucket | Purpose | Owning Sprint |
|--------|---------|---------------|
| `harbor-registry` | Harbor's S3 backend (128 MiB chunk size) | Sprint 4.3 |
| `jitml-checkpoints` | Training checkpoints; one prefix per experiment hash; content-addressed blobs + manifests + ETag-guarded pointers | Sprint 10.1 |
| `jitml-datasets` | Pinned source datasets (MNIST, Fashion-MNIST, CIFAR-10/CIFAR-100 binary archives, Tiny ImageNet Zip64 archive, California Housing archive); SHA-256 verified against the experiment Dhall | Sprint 8.1 |
| `jitml-transcripts` | RL trajectory transcripts | Sprint 8.4 |
| `jitml-trials` | Hyperparameter trial transcripts; content-addressed by `sha256(resolved-dhall || trial-seed)` | Sprint 9.7 |
| `jitml-tensorboard` | TensorBoard event files; the TB pod is stateless | Sprint 4.6 |
| `jitml-artifacts` | Large inference outputs when the demo is in inference mode | Sprint 11.4 |

## Pulsar Topic Family

`<mode>` ∈ `apple-silicon`, `linux-cpu`, `linux-cuda`. The current derived
family has 31 topics: nine substrate-scoped workflow/phase topics across three
substrates, plus Apple-only inference forwarding and host-command topics.

| Topic | Direction | Carrying | Owning Sprint |
|-------|-----------|----------|---------------|
| `training.command.<mode>` | control plane → daemon | `StartTraining`, `StopTraining`, `ResumeFromCheckpoint`, `AbortTraining` | Sprint 8.2 |
| `training.event.<mode>` | daemon → control plane / frontend | `StepDone`, `EpochDone`, `EvalDone`, `CheckpointDone`, `MetricUpdate`, `TrainingFinished`, `TrainingFailed` | Sprint 8.2 |
| `tune.command.<mode>` | control plane → daemon | `RunTrial`, `StopTrial` | Sprint 9.7 |
| `tune.event.<mode>` | daemon → control plane / frontend | `TrialStarted`, `TrialMetricUpdate`, `TrialFinished`, `TrialFailed` | Sprint 9.7 |
| `rl.command.<mode>` | control plane → daemon | `StartRLRun`, `StopRLRun` | Sprint 8.5 |
| `rl.event.<mode>` | daemon → control plane / frontend | `EpisodeDone`, `EvalDone`, `CheckpointDone`, `MetricUpdate` | Sprint 8.5 |
| `inference.request.<mode>` | demo frontend → daemon | inference requests (when demo is in inference mode) | Sprint 11.4 |
| `inference.result.<mode>` | daemon → demo frontend | inference results | Sprint 11.4 |
| `inference.command.apple-silicon` (Apple only) | cluster orchestrator → host daemon | forwarded raw inference-domain command payloads; the host Engine publishes results directly to each request's reply topic | Sprint 7.5 / Sprint 16.12 |
| `training.host-command.apple-silicon`, `tune.host-command.apple-silicon`, `rl.host-command.apple-silicon` (Apple only) | cluster orchestrator → host daemon | typed host-resident Training/RL/Tune command envelopes; the host daemon publishes the normal domain event topics after completion | Sprint 5.11 |
| `gc.event.<mode>` | `jitml internal gc` reconciler → audit / observability | `GcReapedEvent` envelopes (`experiment_hash`, `manifest_sha`, repeated `reaped_blob_shas`, `step_at_reap`, `substrate`, `timestamp_ns`) | Sprint 15.7 |

## `jitml service` Daemon Surface

| Component | Doctrine Section | Status | Owning Sprint |
|-----------|------------------|--------|---------------|
| `BootConfig` Dhall (start-time only; restart-required) | Long-Running Daemons in the Same Binary → BootConfig | ✅ Done | Sprint 5.2 |
| `LiveConfig` Dhall (target hot-reloadable on SIGHUP) | Long-Running Daemons in the Same Binary → LiveConfig; includes log level, retry policy, inference batching/SLO, dedup cache size/TTL, and drain deadline fields; Tart idle/build-VM fields are absent | ✅ Done | Sprint 5.2 / Sprint 5.10 |
| `RunConfig` Dhall (worker run parameters) | Application Environment; Long-Running Daemons in the Same Binary → BootConfig | ✅ Done for Sprint `5.17` — the typed `RunConfig` surface (train/tune/rl params) retired the `JITML_*` run-parameter env IPC in Sprint `5.7`; mounted decode failure now fails closed before any workflow side effect, while developer-side env fallback remains valid only outside dispatched Jobs when no `RunConfig.dhall` mount exists. | Sprint 5.7 / Sprint 5.17 |
| SIGHUP hot reload handler | Long-Running Daemons in the Same Binary | ✅ Done in `src/JitML/Service/HotReload.hs` plus POSIX signal/control wiring in `src/JitML/Service/Signal.hs` | Sprint 5.2 |
| `/healthz` endpoint | Long-Running Daemons → /healthz | ✅ Done as `EndpointResponse` served by `JitML.Service.Runtime.daemonHttpRoutes` | Sprint 5.3 |
| `/readyz` endpoint | Long-Running Daemons → /readyz | ✅ Done as `EndpointResponse` served by `JitML.Service.Runtime.daemonHttpRoutes` | Sprint 5.3 |
| `/metrics` endpoint | Long-Running Daemons → /metrics | ✅ Done as Prometheus text served by `JitML.Service.Runtime.daemonHttpRoutes` | Sprint 5.3 |
| Structured JSON stderr logging | Long-Running Daemons → structured logging | ✅ Done as pure `renderLogEvent`, wired through the in-binary listener | Sprint 5.3 |
| Recoverable-vs-fatal error kinds | Long-Running Daemons → error classification | ✅ Done through `ServiceError` → `AppError` retry mapping | Sprint 5.3 |
| `HasMinIO` capability class (`minioPutIfAbsent`, `minioReadObject`, `minioReadBytes`, `putBlobIfAbsent`, `putBlobBytesIfAbsent`, `casPointer`, `listObjects`, `deleteObject`) plus `ETag` newtype; filesystem-backed instance `JitML.Service.FilesystemMinIO`, subprocess-backed HTTP S3 instance `JitML.Service.MinIOSubprocess`, daemon acquisition settings, the combined `DaemonServiceClient` interpreter in `JitML.Service.Clients`, and `JitML.Service.Workload` checkpoint blob/pointer plus RunInference read effects through parsed dispatcher routing | Capability Classes and Service Errors | ✅ Done for the standalone capability path and BootConfig-derived settings; 2026-05-19 live Linux CPU validation confirms `If-None-Match: *` and `If-Match` conflicts map to `SEConflict` against the routed MinIO edge; 2026-05-20 live service-pod validation confirms the acquired daemon client lists `jitml-checkpoints`; local workload payloads invoke MinIO write/CAS and RunInference reads through the capability class; 2026-05-21 live service-pod validation routes `WriteCheckpointBlob` workload effects through `daemonWorkloadDispatcher`, reads the written objects back from MinIO, and routes `RunInference` through a seeded latest checkpoint in MinIO | Sprint 5.4 |
| `HasPulsar` capability class (`pulsarPublish`, `pulsarAcknowledge`, `pulsarSubscribe`, `pulsarConsume`, `pulsarSeek`) plus `SubscriptionId` newtype and routed one-shot WebSocket instance plus held-open worker surface in `JitML.Service.PulsarWebSocketSubprocess`; `JitML.Service.Clients` derives cluster and Apple-host WebSocket endpoints from `BootConfig` and exposes them through `DaemonServiceClient`; `JitML.Service.Consumer` derives the daemon topic subscription plan from `BootConfig`; `DaemonRuntime` renders the planned subscriptions under `pulsar_subscriptions` and acquisition results under `pulsar_subscription_status` | Capability Classes and Service Errors | ✅ Done for Phase `5`; standalone publish/consume through live Pulsar is validated, daemon settings are derived, local `pulsarSubscribe` planning is tested, `jitml service` crosses the startup subscription-acquisition boundary through a routed zero-queue WebSocket consumer-open probe via `DaemonServiceClient`, and Sprint `5.11` extends the BootConfig-derived Apple host subscription plan to Metal-backed Training/RL/Tune host-command topics. | Sprints 4.4, 5.4, 5.5, 5.11 |
| `HasHarbor` capability class (`harborImageExists`, `harborPromoteImage`, `harborPushImage`, `harborPullImage`, `harborListImages`) | Capability Classes and Service Errors; subprocess-backed explicit local settings live in `JitML.Service.HarborSubprocess`; `JitML.Service.Clients` derives registry/API settings from `BootConfig.harborRegistry`; `JitML.Service.Workload` invokes image promotion through parsed dispatcher routing | ✅ Done for the standalone capability path and BootConfig-derived settings; typed Docker/curl command rendering and live push/pull/list/artifact-existence against Harbor are validated; 2026-05-20 live service-pod validation confirms the acquired daemon client lists the Harbor `library` project; local workload payloads invoke Harbor promotion; 2026-05-21 live service-pod validation routes `PromoteWorkloadImage` payloads into same-repository Harbor tag promotion and verifies the promoted artifact through the in-cluster Harbor API | Sprint 5.4 |
| `HasKubectl` capability class (`kubectlApply` with stdin-piped YAML via `subprocessWithStdin`, `kubectlStatus`, `kubectlGet`, `kubectlDelete`); subprocess-backed instance `JitML.Service.KubectlSubprocess` pins `./.build/jitml.kubeconfig` for host clients and omits `--kubeconfig` for in-cluster service-account clients; `JitML.Service.Clients` carries those settings into `DaemonRuntime`; `JitML.Service.Workload` invokes apply/status/delete through parsed dispatcher routing and maps Linux Training/RL/Tune command envelopes into Kubernetes Job apply/delete effects | Capability Classes and Service Errors | ✅ Done for Phase `5`; typed command shape and daemon settings are validated, Linux CPU/CUDA command envelopes still dispatch to Jobs, and Sprint `5.11` removes Apple Metal-backed Training/RL/Tune starts from the kubectl Job path. Sprint `12.12` owns live no-Apple-Metal-Job assertions. | Sprint 5.4 / Sprint 5.11 |
| `RetryPolicy` typed value | Retry Policy as First-Class Values | ✅ Done; reused by the cluster bootstrap reconciler when its `sh -c` retry/poll loops move to typed Haskell (Sprints 2.9 / 4.8) | Sprint 5.4 |
| At-least-once Pulsar consumer | At-Least-Once Event Processing | ✅ Done; typed dispatcher (`JitML.Service.Consumer.{consumerStep,consumerStepWithActions,runConsumerLoop,consumerOutcomeError}`), fully-qualified topic routing, BootConfig-derived subscription planning, `subscribeDaemonTopics`, startup acquisition (`JitML.Service.Runtime.acquireDaemonSubscriptions`) with zero-queue WebSocket acquisition, bounded acquired-subscription batch (`JitML.Service.Runtime.daemonConsumerBatch`), normal-service held-open workers with one shared process-lifetime `HandlerRouter`, WebSocket subscribe-probe and held-open worker command rendering, command-envelope-to-workload-effect dispatch through `JitML.Service.Runtime.daemonWorkloadDispatcher`, LiveConfig-derived dedup cache size/TTL, TTL-boundary expiry, dispatch-failure seek-without-dedup-poisoning, and lifecycle-exit wiring (`JitML.Service.Runtime.consumerLoopExit`) are validated locally; 2026-05-21 live Linux CPU validation runs the bounded batch from the service pod and acks after dispatch, a second 2026-05-21 live run proves normal service held-open-worker flow by publishing `RunInference` and consuming `InferenceResult` without `--consume-once`, the final live run publishes the same `RunInference` payload twice and observes exactly one matching result, and the dispatch-failure validation negative-acks a missing-checkpoint request until seeding lets broker redelivery complete it | Sprint 5.5 |
| Protobuf-message-hash deduplication keys | At-Least-Once Event Processing → idempotency | ✅ Done as payload SHA-256 event id helper | Sprint 5.5 |
| Stateless `Deployment` (not `StatefulSet`) | Long-Running Daemons | ✅ Done; the stateless Deployment shape is retained and the HA Engine role now enforces one numerical ML compute worker per compute scope per Kubernetes node while non-compute replicas may scale independently. | Sprint 5.6 / Sprint 5.16 |
| Required pod anti-affinity (`topologyKey: kubernetes.io/hostname`) | Long-Running Daemons | ✅ Done; service Engine pods use `jitml.compute-scope=service` and daemon-spawned Linux workload Jobs use `jitml.compute-scope=workload`, with required hostname anti-affinity and hard topology spread scoped to each selector. | Sprint 5.6 / Sprint 5.16 |

## Numerical Core Inventory

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Layer catalog (16: Dense, Embedding, Conv1D, Conv2D, Conv3D, ConvTranspose, ComplexDense, ComplexConv2D, BatchNorm, LayerNorm, GroupNorm, Dropout, ResidualBlock, ScaledDotProductAttention, MultiHeadAttention, RotaryPositionalEmbedding) | `src/JitML/Numerics/Catalog.hs`; rendered through `renderNumericalCatalog` | ✅ Done | Sprint 6.1 |
| Real-valued activations (8: Relu, LeakyRelu, Elu, Silu, Gelu, Tanh, Sigmoid, Softmax) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.2 |
| Complex-valued activations (3: ComplexModRelu, ComplexCardioid, ComplexZRelu) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.2 |
| Spectral / frequency-domain ops (10: FFT, FFTAlongAxis, IFFT, IFFTAlongAxis, RFFT, IRFFT, STFT, DCT, ComplexConjugate, ComplexMatMul) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.3 |
| Optimizers (13: SGD, MomentumSGD, NesterovSGD, RMSProp, Adagrad, Adadelta, Adam, AdamW, LAMB, LARS, Lion, AdaFactor, Shampoo) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.4 |
| Schedulers (9: Constant, Linear, Cosine, CosineWithWarmup, Exponential, Polynomial, OneCycle, Piecewise, ReduceOnPlateau) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.4 |
| Loss functions (10: CrossEntropy, BinaryCrossEntropy, SparseCrossEntropy, Focal, MSE, Huber, IoU, Dice, KLDiv, Contrastive) | `src/JitML/Numerics/Catalog.hs` | ✅ Done | Sprint 6.5 |
| Dhall numerical mirror and cross-type audit | `dhall/numerics/`, `src/JitML/Numerics/Schema.hs`, `src/JitML/Lint/DhallNumerics.hs`; reflected emission via `src/JitML/Service/CatalogSchema.hs` (`jitml internal dhall-schema --catalog numerics\|rl\|all`) with a `jitml-unit` file ≡ emitted parity case | ✅ Done; reflected catalog emission landed 2026-06-23 (numerics + RL leaves emitted from the Haskell catalogs, drift fails in both directions) | Sprint 6.6 / Phase 5 |
| Experiment Dhall fixtures that exercise the catalog | `experiments/mnist.dhall`, `experiments/mnist-tune.dhall`, `experiments/cartpole.dhall` | ✅ Done | Sprint 6.6 |

## JIT Codegen Components

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Apple Silicon engine (Metal codegen + fixed bridge) | `src/JitML/Engines/{MetalBridge,MetalRuntime,MetalLocal,HasEngine,Loader}.hs` plus `src/JitML/Codegen/{RuntimeSource,Metal,MlpMetal}.hs`; Haskell renders MSL + launch metadata, persists `./.build/jit/apple-silicon/<hash>.metal.json`, and the fixed bridge compiles/dispatches through `MTLDevice.makeLibrary(source:options:)` with fast math off. MLP forward/backward/batch entrypoints route generated MSL through the same bridge ABI. No generated Swift package, Tart `swift build`, generated dylib copy-out, or per-kernel Apple `dlopen` path remains in the core cache miss. | ✅ Done; Sprint `7.11` landed the fixed bridge and Sprint `16.9` live-validated it on Apple Silicon | Sprints 7.5, 7.11, 16.2, 16.5, 16.9 |
| Linux CPU engine (oneDNN codegen + local FFI fixture) | `src/JitML/Engines/{CpuFeatures,Engine,HasEngine,Loader,Local,OneDnnRuntime,TuningBenchmark}.hs`, `src/JitML/Codegen/{RuntimeSource,OneDnn}.hs`; generated oneDNN C++ inputs under `./.build/jit-src/linux-cpu/<hash>/`; local identity/reorder, reduction, dense/matmul, convolution, normalization, attention/matmul, and embedding/reorder kernels compile with `g++ ... -ldnnl` through the shared cache artifact loader, load with `dlopen`, run through the Haskell FFI, report the loaded `jitml_kernel_family_name` plus `jitml_kernel_output_count`, dispatch generated-family requests through the local Linux CPU `HasEngine` interpreter, include `artifact-abi=<os>-<arch>` and `reduction-block=256` in the local toolchain fingerprint, probe AVX2/AVX-512 through typed subprocesses for the oneDNN micro-kernel knob, probe `libdnnl` package/header/link visibility through typed subprocesses, and measure Linux CPU benchmark candidates through generated FFI output digests | ✅ Done for Sprint 7.3; Sprint 7.6 owns first-cache-miss benchmark invocation and within-substrate run-to-run equality | Sprints 7.3, 7.6, 7.7 |
| Linux CUDA engine (CUDA C codegen) | `src/JitML/Engines/{CudaLocal,CudaRuntime,Engine,HasEngine,Rng}.hs`, `src/JitML/Codegen/{RuntimeSource,Cuda}.hs`; generated CUDA inputs under `./.build/jit-src/linux-cuda/<hash>/`; host SplitMix64 RNG, generated no-curand metadata, deterministic no-atomic reduction source, host-callable CUDA `jitml_kernel` wrapper with device-buffer launch/copyback, guarded CUDA local loader, `LocalCudaEngine` metadata-checking dispatch, host reduction partial-count/finalization helpers, typed `nvcc` / `nvidia-smi` / `ldconfig` CUDA runtime probes, generated family/output-count metadata, and 2026-05-27 Sprint 15.11-closing per-family weighted CUDA bodies for Conv2D / Conv3D / BatchNorm / LayerNorm / Embedding / MHA + 2026-05-27 Sprint 15.12-closing live `jitml inference run` end-to-end (nvcc → `.so` → dlopen → device kernel → MinIO read → Pulsar publish) on the RTX 3090 cluster; 2026-05-28 Sprint 15.8/15.9 nvcc forward/backward MLP network kernels (`src/JitML/Codegen/MlpCuda.hs` + `src/JitML/Numerics/MlpCuda.hs`) GPU-validated 3/3 in `jitml-backends` (forward + backward match the pure `JitML.Numerics.Mlp` within `1e-3`, bit-deterministic run-to-run) | ✅ Done (Phase 7 code surface; live CUDA path re-validated on the RTX 5090). The end-to-end evidence (MLP forward/backward kernels, weighted families, `jitml inference run`) was first recorded on an RTX 3090 and re-exercised on the current RTX 5090 (compute capability `12.0` / `sm_120`; `-arch=sm_70` PTX JIT-compiles onto Blackwell at launch, so no `-arch` bump is required); the `linux-cuda` lane of `jitml-backends` re-validated 19 / 19 on 2026-06-09 (Sprint 15.16). See [phase-15 → Phase Status](phase-15-linux-cuda-and-cluster-closure.md). | Sprints 7.4, 7.7, 15.11, 15.12 |
| Haskell-owned runtime JIT source generation | `src/JitML/Codegen/{RuntimeSource,Cuda,MlpCuda,OneDnn,Metal,SourceFile}.hs`; no checked-in generated compiler-input directories | ✅ Done | Sprint 7.7 |
| Kernel handle, cache hit/miss, artifact loader, and engine envelope surface | `src/JitML/Engines/{Engine,Loader}.hs`; `KernelHandle`, `JitCacheStatus`, `KernelArtifact`, `KernelInputs`, `KernelOutputs`, `EngineEnvelope` | ✅ Done | Sprints 7.1, 7.2 |
| Content-addressed cache key shape | `src/JitML/Cache/Key.hs`; `KernelSpec` hashes `(canonical payload, kind, substrate, toolchain-fingerprint, rendered-source payload, tuning choice)` | ✅ Done | Sprints 2.3, 7.7 |
| Cache root layout | `src/JitML/Cache/Layout.hs`; `./.build/jit/<substrate>/<hash>.<ext>` plus `manifest.json` path resolution | ✅ Done | Sprint 2.3 |
| Cache manifest index | `src/JitML/Cache/Manifest.hs`; JSON round-trip, lookup, upsert, read, and atomic write helpers | ✅ Done | Sprint 2.3 |
| Apple generated-dylib symlink surface | Deleted (`src/JitML/Cache/Symlink.hs` removed). Apple uses the process-stable fixed bridge plus `.metal.json` source metadata; Linux shared-object loading is unchanged. | ✅ Deleted by Sprint `7.11` | Sprint 7.11 |
| Hardware auto-tuning | `src/JitML/Engines/{Tuning,TuningBenchmark,TuningStore,TuningCache}.hs`; `TuningChoice` is a cache-key input and generated-source metadata string; per-substrate knob spaces, deterministic defaults, deterministic-only cuDNN choices, stable benchmark candidate plans, pure measured-result selection/ranking, generic measurement collection with SHA-256 output digests, Linux CPU oneDNN FFI candidate measurement, guarded CUDA/Metal runtime-preflight runner boundaries, JSON persistence of selected measured choices by substrate/base hash, and persisted-choice cache-key derivation are checked in | ✅ Done; Linux CPU / CUDA / Metal candidate runners are guarded by runtime probes, and live CUDA/Metal candidate execution is validated when the matching device is visible | Sprint 7.6 / Sprint 15.15 / Sprint 16.3 |
| Apple Silicon host↔cluster inference contract | `src/JitML/Proto/Inference.hs`, `src/JitML/Service/Runtime.hs`, and `proto/jitml/inference.proto` define the values-model forward: the cluster forwards raw `RunInference` / `CheckpointCompareCommand` / `AdversarialMoveCommand` payloads to `inference.command.apple-silicon`, and the host Engine publishes the matching result directly to the request's reply topic. Sprint `5.11` adds the parallel host-command topic family for Metal-backed Training/RL/Tune starts. | ✅ Done; the superseded Apple refs/event RPC is removed by Sprint `16.12`, the values-model forward is live-validated, Sprint `5.11` extends the contract to Apple host-resident Training/RL/Tune command starts, and Sprint `16.10` validates the full Apple lane through those host-command subscriptions. | Sprint 7.5 / Sprint 16.4 / Sprint 5.11 / Sprint 16.10 / Sprint 16.12 |

## Training Workload Surfaces

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Supervised training summaries | `src/JitML/SL/{Canonicals,Classifier,Architecture,Dataset}.hs`; command surface in `src/JitML/App.hs` decodes staged dataset bytes, resolves the row's `ArchitectureSpec`, and trains every canonical SL architecture through the selected substrate `MlpDevice`; Dense, DeepDense, patch-Conv2D/LeNet, residual, wide-residual, ResNet-50, patch-attention, and California regression rows all have substrate-backed train-step coverage, and the live linux-cpu Sprint `8.12` matrix materializes every canonical row from SHA-pinned staged MinIO bytes | ✅ Done; SL runtime rows emit convergence stats into `CompletedTraining` witnesses and prove checkpoint inference eligibility in the `linux-cpu` fixed-budget baseline | Sprint 8.1 / Sprint 8.12 / Sprint 8.14 / Sprint 15.4 / Phase 13 |
| Canonical SL problem set | `src/JitML/SL/Canonicals.hs`; deterministic coverage in `test/sl-canonicals/Main.hs`; `trainableCanonicalCohort` covers all eleven rows and is the product cohort; live MNIST data staging/training is exercised through `jitml internal upload-dataset`, `jitml train`, and the live SL canonical case; Fashion-MNIST image/label pins, CIFAR-10/CIFAR-100 archive pins plus archive-backed binary-batch parser coverage, California Housing archive pin plus archive-backed regression-row parser/device-MSE-trainer coverage, and Tiny ImageNet archive pin plus Zip64/JPEG tensor materialization coverage are present; the focused linux-cpu live 8.12 matrix fetched all staged artifacts from MinIO and ran bounded train/eval for every row | ✅ Done; the product cohort, real artifacts, completed fixed-budget trained artifacts, and model-owned convergence statistics are closed for the `linux-cpu` baseline | Sprint 8.1 / Sprint 8.12 / Sprint 8.14 / Sprint 15.4 / Phase 13 |
| Canonical RL environments (cartpole, mountain-car, lunar-lander, KeyDoorGrid; optional atari-subset) | `src/JitML/RL/Environments.hs`; `src/JitML/RL/Simulator.hs`; `src/JitML/RL/SimulatorLoop.hs`; `src/JitML/RL/ALE.hs`; deterministic local step surface and catalog renderer for native simulators. `KeyDoorGrid-v0` is the repo-owned visual discrete-control environment for default demos and required canonical examples, with deterministic seeded maps, legal-action masks, vector/grid observations, generated render frames, and checked-in `experiments/key-door-grid.dhall` coverage. `atari-subset` remains optional runtime support only when a generated or externally supplied ALE runtime shim is available, with ROMs supplied explicitly through `RunConfig.atariRomPath`, `JITML_ATARI_ROM`, or `JITML_ALE_ROM`; local ROM files belong under ignored `./.roms/`. The repository has no checked-in C/C++ ALE adapter source; all project-owned native/JIT compiler inputs are Haskell-generated into the build/cache tree. | ✅ Done for native canonical environments and default copyright-free visual demo coverage; optional ALE execution remains manual/runtime-only. | Sprint 8.3 / Sprint 8.8 / Sprint 8.9 |
| RL Algorithm catalog and local rollout surface | `src/JitML/RL/Algorithms.hs`; `dhall/rl/Schema.dhall` mirror/audit; measured command summaries in `src/JitML/App.hs`; typed runtime primitives in `src/JitML/RL/{Policy,VecEnv,Buffer,AsyncBuffer,Loop}.hs` with filesystem-backed async-sink coverage; live daemon-dispatched environment execution publishes `rl.event.<substrate>` episodes; Sprint `9.12` removes algorithm-level reward-derived projections and writes `.jmw1` checkpoints plus replay artifacts from `jitml rl train` / `jitml rl rollout` | ✅ Done; algorithm and AlphaZero game paths carry fixed budgets, completion witnesses, convergence metrics, checkpoint eligibility, and browser/e2e proof in the `linux-cpu` baseline | Sprint 8.4 / Sprint 9.12 / Sprint 9.14 / Sprint 15.5 / Phase 13 |
| Replay/rollout trajectory determinism | `jitml-rl-canonicals` compares fresh same-seed real trainer rollouts over named environment dynamics, live RL events exercise real stepping through the daemon path, and typed replay payloads are consumable by browser animations | ✅ Done for product closure; the browser replays trajectories produced by completed fixed-budget trained artifacts in the full model matrix | Sprint 8.4 / Sprint 11.9 / Sprint 15.5 / Phase 14 |
| Schedules, action distributions, action noise, target networks, GAE, callbacks, logger, evaluator | `src/JitML/RL/Framework.hs`; typed framework catalog, run plan, evaluator, and callback surfaces | ✅ Done | Sprint 8.5 |
| Training loops as typed pipelines | Plan/Apply command rendering in `src/JitML/Plan/Plan.hs` and `src/JitML/App.hs`; `RL.Loop.{RLLoop,RLConfig,runRLLoop}` records local RL rollout transitions in `ReplayBuffer`. `JitML.SL.Classifier` owns IDX/CIFAR decode and the classifier primitive, `JitML.SL.TinyImageNet` owns Zip64/JPEG materialization, `JitML.SL.Regression` owns California Housing standardization and MSE training, and `JitML.SL.Architecture` owns all-row device-backed classifier training over decoded examples; `RunConfig` controls live worker budgets; `JitML.SL.ConvergenceThresholds` owns the in-code live threshold table and `jitml-sl-canonicals` carries live MNIST convergence, all-row train-step assertions, and the focused live all-row staged-byte train/eval smoke; `JitML.Training.Budget` defines the pure budget/completion witness consumed by checkpoint eligibility, and the SL/RL/tuning event payloads now carry optional completed-training witnesses | ✅ Done; witness emission, fixed-budget completion, inference rejection, and browser consumption have all-row/live `linux-cpu` proof | Sprints 8.6, 8.12, 8.14, 15.4 / Phase 13 |
| Train/test/validation splits + convergence-and-performance metrics | `src/JitML/SL/Architecture.hs` (`trainArchitectureWithDeviceSelected`, `crossEntropyArchitectureWithDevice`, `SlRunMetrics`), `src/JitML/App.hs` (`TrainingMetrics`, `splitTrainValidation`), `src/JitML/SL/ConvergenceThresholds.hs`, `src/JitML/RL/ConvergenceThresholds.hs` (`FixedBudgetRlConvergenceRow`, `HerGoalMetric`, `AlphaZeroGameConvergenceRow`), and the SSoT `documents/engineering/training_metrics_and_splits.md`; SL uses a three-way split (validation drives selection/early-stop, test held-out), a real cross-entropy/MSE loss, and a non-wall-clock `examples_processed` throughput metric; RL now has pure fixed-budget rows for all advertised algorithms, HER goal metrics, and per-game AlphaZero arena/legal-move/MCTS metrics | ✅ Done; runtime-emitted fixed-budget convergence stats and checkpoint-integrated completion evidence are implemented for the `linux-cpu` baseline | Sprint 8.13 / Sprint 8.14 / Sprint 9.13 / Sprint 9.14 / Sprint 13.2 / Sprint 13.3 |
| RL algorithm catalog (15 entries: 5 OnPolicy — PPO, A2C, TRPO, MaskablePPO, RecurrentPPO; 5 OffPolicy — DQN, QR-DQN, DDPG, TD3, SAC; 4 Specialized — CrossQ, TQC, ARS, HER; 1 SelfPlay — AlphaZero) | `src/JitML/RL/Algorithms.hs` (catalog); one module per algorithm under `src/JitML/RL/Algorithms/{Ppo,A2c,Trpo,MaskablePpo,RecurrentPpo,Dqn,QrDqn,Ddpg,Td3,Sac,CrossQ,Tqc,Ars,Her}.hs` aggregated by `Registry.algorithmModuleRegistry`. The algorithmic seam includes `JitML.Numerics.Mlp`, real on-policy/off-policy/continuous/ARS/HER trainers, `PolicyValueNet` for AlphaZero, `jitml rl train` dispatch through `App.runTrainerEpisodes`, `.jmw1` checkpoint writes for trained weights, replay/transcript artifact writes, nvcc forward/backward CUDA kernels for the `JitML.Numerics.Mlp` interface, and live daemon-dispatched RL / AlphaZero validation; Sprint `9.12` removes remaining reward-derived projection helpers while Phase `13` consumes trained-policy artifacts for train/eval/rollout/self-play | ✅ Done; per-algorithm fixed-budget convergence, checkpoint eligibility, and UI/e2e consumption evidence are present in the `linux-cpu` baseline | Sprints 9.1–9.5, 9.12, 9.14, 15.8, 15.9 / Phase 13 |
| Substrate MLP device seam (`mlpDeviceForSubstrate` / `rlDeviceForSubstrate`) | `src/JitML/Numerics/MlpDeviceSelect.hs` resolves the CLI `--substrate` to the JIT-compiled `MlpDevice` (`oneDnnMlpDevice` / `cudaMlpDevice` / `metalMlpDevice`); `rlDeviceForSubstrate` is the single DRY seam the 13 MLP-backed RL trainers route through. **ARS is the lone no-MLP exception** (finite-difference random search, no network forward/backward). `App.runTrainerEpisodes` probes the device (`probeMlpDevice`) and fails closed before dispatch; SL architecture training, device-backed AlphaZero MCTS leaf evaluation, and device-backed tuning trials route through the same seam. `JitML.SL.Architecture` composes batched forward, batch-gradient, and input-gradient calls so the full canonical SL cohort is no longer gated by `denseMlpCohort` | ✅ Done for the device-selection seam; all-model artifact closure above remains `✅ Done` and cannot be inferred from seam existence alone | Sprints 8.10 / 8.11 / 8.12 / 9.10 / 9.11 / Phase 13 |
| RL correctness checks (run-to-run determinism + statistical convergence + property tests) | deterministic local tests in `test/rl-canonicals/Main.hs` compare two fresh runs against each other on the same substrate/seed plus property assertions; live PPO/cartpole convergence and `RunConfig` step/eval episode consumption are validated through daemon-dispatched jobs (no committed trajectory or reward-distribution files per [../README.md → Snapshot targets → Numerical-fixture prohibition](../README.md#snapshot-targets)); Sprint `9.12` broadens this to every algorithm and game workflow without synthetic helpers | ✅ Done; deterministic/property foundations, RL fixed-budget convergence coverage, and infer-before-complete negative checks are validated for the `linux-cpu` baseline | Sprint 9.4 / Sprint 9.12 / Sprint 9.14 / Sprint 12.15 / Sprint 15.6 / Phase 13 |
| AlphaZero-style self-play and persistent MCTS state | `src/JitML/RL/AlphaZero.hs` provides game state/move helpers, `GameOutcome` terminal/winner/draw rules for Connect 4/Othello/Hex/Gomoku, and arena summaries; `src/JitML/RL/AlphaZero/{Mcts,SelfPlay,PolicyValueNet}.hs` carry the recursive PUCT search tree (visit-count + transposition table), self-play buffer with `bufferTranscriptHash` and CBOR round-trip, arena win-rate promotion measurement, the two-headed policy/value network (`networkPolicyValue`, `mctsVisitDistribution`, `mctsVisitDistributionWithDevice`, `trainPolicyValueNetOnSamples`), position-aware pure `PriorOracle` wiring from the real network forward pass via `runSelfPlayWithOracleFactory`, effectful `PriorOracleIO` wiring for substrate-backed leaf evaluation, `.jmw1` checkpoint writes from `jitml rl alphazero self-play`, and `alphazero-transcript` artifacts containing sampled states, MCTS visit distributions, and outcome labels | ✅ Done; each game has fixed-budget arena convergence stats, inference-eligible policy/value checkpoints, and browser replay proof in the `linux-cpu` baseline | Sprint 9.5 / Sprint 9.10 / Sprint 9.12 / Sprint 9.14 / Sprint 15.9 / Phase 14 |
| Perfect-information game, two-headed network, and arena summary surface | `src/JitML/RL/AlphaZero.hs`; canonical game catalog, per-game terminal/winner/draw evaluators, Connect 4 two-headed network metadata, policy/value network training, and arena summary helpers; Sprint `14.1` requires MCTS/value traces and interactive replay payloads for every adversarial game | ✅ Done for product closure; value/MCTS traces render from completed per-game trained artifacts | Sprint 9.5 / Sprint 9.12 / Sprint 17.2 / Phase 14 |
| Canonical adversarial games (Connect 4, Othello, Hex, Gomoku) | `src/JitML/RL/AlphaZero.hs` `canonicalGames` lists all four; `initialConnect4`, `initialOthello`, `initialHex`, `initialGomoku` plus per-game `applyMove` rules, `GameOutcome` terminal/winner/draw evaluators, pass-aware Othello replay, and per-game two-headed network metadata (`connect4Network`, `othelloNetwork`, `hexNetwork`, `gomokuNetwork`); the typeclass `PerfectInformation` admits all four games; per-game self-play and terminal evaluation are exercised by `jitml-rl-canonicals` as run-to-run determinism plus rule-conformance properties; Sprint `14.1` adds browser render/replay/step controls for all four games | ✅ Done for no-caveat closure; rules/typeclass coverage, completed fixed-budget self-play artifacts, and UI/e2e proof are present for all four games | Sprint 9.6 / Sprint 9.12 / Sprint 9.14 / Sprint 14.1 / Sprint 14.4 |
| Hyperparameter tuning (sampler × scheduler × pruner) | `src/JitML/Tune/Catalog.hs` covers the full target sampler catalog (`Grid`, `Sobol`, `Random`, `TPE`, `GPBO`, `GeneticAlgorithm`, `NSGA2`, `MuLambdaES`, `CMAES`, `EvolutionStrategies`, `PBT`), Fifo/SuccessiveHalving/Hyperband/ASHA, and none/median/percentile catalogs; `experiments/mnist-tune.dhall` decodes to the local TPE / ASHA / MedianPruner ADT and drives the current `jitml tune` renderer; `trialObjectiveResultWithDevice` trains each sampled trial through the selected JIT `MlpDevice` and returns checkpointable weights; local `jitml tune` promotes the best preview trial into `.jmw1`, emits a `tune-trials` artifact, and live worker Jobs promote measured trial weights into `jitml-checkpoints` while retaining `jitml-trials` transcripts; `JitML.Proto.Tune` covers text/proto3-compatible command-envelope render/parse. Apple trials are host-resident after Sprint `5.11`; Phase `14` owns browser sweep control/visualization | ✅ Done; promoted checkpoints carry fixed trial-budget completion, objective/convergence stats, inference eligibility, and browser-visible TensorBoard/checkpoint evidence | Sprint 9.7 / 9.11 / 9.12 / Sprint 10.10 / 15.10 / 16.10 / Phase 14 |
| Trial storage and resume summary surface | `src/JitML/Tune/Catalog.hs` (key/summary helpers) + `src/JitML/Tune/Resume.hs` (`persistTrialTranscript`, `replaySweep` over `HasMinIO`) | ✅ Done; resume round-trip is validated against filesystem-backed `HasMinIO` and live MinIO persistence/replay is validated by `jitml-integration` | Sprint 9.7 / Sprint 15.10 |

## Checkpoint and Inference Components

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Storage layout (typed) | `src/JitML/Checkpoint/Format.hs` plus bucket registry in `src/JitML/Storage/Buckets.hs`; `CheckpointManifest` carries tensor/optimizer/RNG blobs, monotonic step, metrics, parent lineage, deterministic CBOR/content hash, and typed object-key renderers | ✅ Done; manifests carry budget, completion witness, convergence stats, readiness, and TensorBoard metadata needed to mint inference-eligible checkpoints | Sprint 10.1 / Sprint 10.10 / Sprint 15.7 |
| Split-blob layout (`blobs/<sha256>`, `manifests/<sha256>`, `pointers/{latest,best/<metric>,trial/...}`) | `src/JitML/Checkpoint/Format.hs`; typed object-key renderers for blobs, manifests, latest, best, and trial pointers | ✅ Done | Sprint 10.1 |
| `.jmw1` dense weight blob format | `src/JitML/Checkpoint/Format.hs`; `encodeJmw1` writes `JMW1`, a CBOR header length/header, and little-endian `F64` payload bytes, and `decodeJmw1` validates/loads the same local payload shape. Sprint 15.9 (2026-05-28) added the trained-network weight surface: `Mlp.{mlpParamsToFlat,mlpParamsFromFlat}` + `PolicyValueNet.{policyValueNetToFlat,loadPolicyValueNetWeights}` flatten / reconstruct a trained AlphaZero network through this blob, with a `jitml-rl-canonicals` bit-identical round-trip | ✅ Done | Sprints 10.2, 15.9 |
| Manifest pointer surface | `src/JitML/Checkpoint/Format.hs`; `CheckpointManifest`, deterministic manifest CBOR codec/content hash, `manifestPointer` | ✅ Done; latest/best/trial pointers refuse promotion to inference unless the manifest has `CompletedTraining` and passing convergence evidence | Sprint 10.2 / Sprint 10.10 |
| Write-once and CAS protocol surface | `src/JitML/Checkpoint/{Format,Store}.hs`; pure pointer-write CAS decision surface plus filesystem-backed write-once object/manifest writes, latest-pointer CAS, and `writeCheckpointSnapshotWithMinIO` over `HasMinIO.putBlobBytesIfAbsent` / `casPointer`; `JitML.Service.MinIOSubprocess` live-validates the underlying `HasMinIO` conditional-write effects | ✅ Done; live checkpoint snapshot write/idempotent rewrite and MinIO conditional-write behavior are validated | Sprint 10.2 / Sprint 15.7 |
| Within-substrate bit-determinism contract | [../documents/engineering/determinism_contract.md](../documents/engineering/determinism_contract.md) | ✅ Done (Sprint `17.4`); cross-substrate equivalence is out of contract — the cross-substrate tolerance methodology, `src/JitML/Engines/Tolerance.hs`, and `JitML.CrossBackend.Parity` are removed (landed 2026-06-09; the doc is reframed to within-substrate-only). Within-substrate run-to-run bit-identity is validated per substrate by `jitml-backends` in each lane: `apple-silicon` 4 / 4, `linux-cpu` 10 / 10, and `linux-cuda` 19 / 19 all re-validated 2026-06-09 (the linux-cuda lane on the RTX 5090, Sprint `15.16`) | Sprint 10.3 / 17.4 |
| Inference-only read path | `src/JitML/Checkpoint/{Format,Store}.hs`, `src/JitML/Engines/Local.hs`, `src/JitML/Proto/Inference.hs`, and `proto/jitml/inference.proto`; `CheckpointManifest` carries model-family, architecture, preprocessing, output-decoder, weight-layout, replay/transcript, substrate-artifact metadata, and optional `CompletedTraining`. `loadInferenceCheckpointWith` provides an explicit injected-runner hook, while `loadInferenceCheckpointWithWeights` covers latest-pointer → manifest → `.jmw1` weight blob loading/decoding for generated weighted-kernel runners. The loaders validate manifest experiment/content SHA compatibility, call `requireInferenceEligibleCheckpoint`, and reject decoded weight payloads whose length disagrees with the tensor shape. Default manifest-only inference fails closed (`weighted inference runner required`), proto3-compatible byte round-trips are covered in `jitml-daemon-lifecycle`, and live user-facing `jitml inference run` routes through weighted MinIO-backed checkpoint inference. | ✅ Done; endpoint-local opaque API boundaries and negative coverage reject partial/smoke/random/uncompleted manifests before inference | Sprint 10.4 / Sprint 10.6 / Sprint 10.10 / Sprint 15.12 / Phase 13 |
| Retention reconciler (`jitml internal gc <experiment-hash>`) | `src/JitML/Checkpoint/Store.hs` (`walkLiveSet`, `applyRetentionPolicy`, `buildGcPlan`, `executeGcPlan`) plus `src/JitML/App.hs` no-op exit `3` command wiring and live MinIO/Pulsar execution | ✅ Done; live HTTP MinIO blob deletion and `gc.event.<substrate>` publication are validated | Sprint 10.3 / Sprint 15.7 |
| TensorBoard checkpoint sidecar (`<step>-<manifest-sha>.cbor` under `jitml-tensorboard/<experiment-hash>/checkpoints/`) | `src/JitML/Observability/TensorBoard.hs` (key + CBOR encoder); `src/JitML/Observability/TbSidecar.hs` (`writeCheckpointSidecar`, `dispatchCheckpointPayload` over `HasMinIO`); `src/JitML/Service/Runtime.hs` (`daemonTensorBoardDispatcher`) | ✅ Done; sidecar transport publishes completion counters, convergence stats, readiness, and inference eligibility into TensorBoard-visible checkpoint metadata | Sprint 4.6 / Sprint 10.10 / Sprint 14.4 |

## Frontend Components

| Component | Implementation | Status | Owning Sprint |
|-----------|----------------|--------|---------------|
| Minimal PureScript application | `web/src/Main.purs` | ✅ Done | Sprint 11.1 |
| Browser-contract source ADTs | `src/JitML/Web/Contracts.hs` renders `web/src/Generated/Contracts.purs` through the local bridge-compatible renderer; Sprint `11.9` expands the generated contract from endpoint metadata to typed inference/generic/image/checkpoint-compare/adversarial REST request renderers, result/event payload records and parsers, `WorkflowStatus`, and browser command-envelope renderers consumed by the current panels | ✅ Done; contracts expose training budget, completion, convergence, TensorBoard run, and inference-eligibility state to the UI | Sprint 11.2 / Sprint 11.9 / Sprint 11.11 / Phase 14 |
| Generated PureScript contracts | `web/src/Generated/Contracts.purs`; the contract renderer is covered by the PureScript lint target; Sprint `11.9` regenerates the current payload/event/control surface, renders browser inference/generic/image/checkpoint-compare/adversarial request envelopes, renders daemon-compatible training/RL/tune start/stop command envelopes with a live-substrate token, parses `WorkflowCommandAck`, renders/parses `WorkflowStatus`, and rejects hand-maintained parser drift. Sprint `11.11` adds trained-artifact fields to `CheckpointSummary` (`eligibility`, completed budget, convergence metrics, TensorBoard prefix) and regenerates the PureScript contract from Haskell. | ✅ Done; generated fields are rendered by panels and asserted by Playwright for every model row in the `linux-cpu` baseline | Sprint 11.2 / Sprint 11.9 / Sprint 11.11 / Phase 14 |
| PureScript lint checks | `web/test/`, `web/spago.yaml`, `src/JitML/Lint/Stack.hs`; current lint target checks generated-contract presence, recursive checked-in source whitespace shape, panel-contract coverage, explicit typed `spago test` + `purs-tidy check` command shapes, and the `spec-node` `purescript-spec` smoke suite | ✅ Done; 2026-06-04 `spago test` passed 7 / 7 with zero PureScript warnings after replacing the deprecated `runSpec` alias | Sprint 11.3 |
| Playwright scaffold | `playwright/jitml-demo.spec.ts`; the spec reads `.build/runtime/cluster-publication.json`, fails fast when no live publication exists, and drives the portals/header/admin-link/eight-panel matrix through the real edge route with typed REST response assertions for the current REST panels; `jitml-e2e` validates the typed Playwright plan and server-side route/concurrency invariants; Sprint `12.13` / `14.2` replace panel reachability checks with a no-caveat product matrix that starts workflows, waits for training/eval/checkpoint evidence, exercises interactions, validates RL animations, and replays adversarial games | ✅ Done; live Playwright covers every trained-artifact model interaction plus infer-before-complete rejection for the `linux-cpu` baseline | Sprint 11.6 / Sprint 11.7 / Sprint 12.13 / Sprint 14.4 / Sprint 15.14 / Sprint 17.3 / Phase 14 |
| Bundle output | `src/JitML/Web/Bundle.hs`; typed bundle asset manifest and full demo route manifest for the generated PureScript bundle output paths; `src/JitML/Web/Server.hs` serves `web/dist/Main/bundle.js` as `/bundle/main.js` when the file exists and routes `/api/ws`, `/api/ws/training`, `/api/ws/rl`, and `/api/ws/tune` through the WebSocket bridge | ✅ Done; `jitml-e2e` covers the bundle route and plain HTTP stream 503s, and 2026-06-04 live Playwright validated the baked bundle through the edge route | Sprint 11.4 / Sprint 15.13 / Sprint 17.3 |
| MNIST live inference panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `InferenceRun`; `web/src/Panels/Mnist.purs` posts `Generated.Contracts.renderBrowserInferenceRequest` to `/api/inference` and parses `Generated.Contracts.InferenceResult` with model id, checkpoint SHA, latency, probabilities, and output instead of `prediction:` markers; `JitML.Web.Server` routes that envelope through an injected checkpoint runtime handler when present | ✅ Done; the panel rejects non-eligible checkpoints and displays fixed-budget convergence/completion evidence from the selected trained artifact | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.14 / Phase 14 |
| Generic tensor inference panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `GenericInference`; `web/src/Panels/GenericInference.purs` posts `Generated.Contracts.renderBrowserGenericInferenceRequest` to `/api/inference/generic` and parses `Generated.Contracts.GenericInferenceResult` with checkpoint SHA, latency, output tensor, and status; `JitML.Web.Server` routes that envelope through the injected checkpoint runtime handler when present | ✅ Done; live endpoint wiring is driven from completed fixed-budget artifacts and surfaces eligibility/convergence metadata | Sprint 11.9 / Sprint 11.11 / Phase 14 |
| CIFAR/ImageNet upload panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `UploadImage`; `web/src/Panels/Cifar.purs` posts `Generated.Contracts.renderBrowserImageRequest` to `/api/images` and parses `Generated.Contracts.ImageInferenceResult` with checkpoint SHA, top-k, probability, preprocessing, and inference timing fields instead of `image:` markers; `JitML.Web.Server` routes that envelope through an injected checkpoint runtime handler when present | ✅ Done; image-model output comes from eligible trained artifacts for the relevant SL rows | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.14 / Phase 14 |
| Checkpoint compare panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `CheckpointCompare`; `web/src/Panels/CheckpointCompare.purs` posts `Generated.Contracts.renderBrowserCheckpointCompareRequest` to `/api/checkpoints/compare` and parses `Generated.Contracts.CheckpointCompareResult` with baseline/candidate checkpoint SHAs, output tensors, max absolute delta, mean absolute delta, latency, and status; `JitML.Web.Server` performs two injected checkpoint runtime calls and computes deltas from the returned tensors | ✅ Done; both sides are `InferenceEligibleCheckpoint` values with visible budget/convergence provenance | Sprint 11.9 / Sprint 11.11 / Phase 14 |
| AlphaZero adversarial-game panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `Connect4Move`; `web/src/Panels/Connect4.purs` posts `Generated.Contracts.renderBrowserAdversarialMoveRequest` to `/api/connect4/move`, parses `Generated.Contracts.AdversarialMoveResult`, selects Connect 4/Othello/Hex/Gomoku, and renders the selected board from the move transcript plus legal moves, visit counts, policy priors, value estimate, transcript id, game-over state, per-game rule summaries, legal-action counts, and local transcript prev/next scrub controls instead of a `move:` marker; `JitML.Web.Server` routes that envelope through an injected checkpoint runtime handler and derives MCTS priors/value from its weighted output when present | ✅ Done; Othello/Hex/Gomoku/Connect 4 use completed self-play artifacts rather than seeded policy/value stand-ins | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.14 / Phase 14 |
| RL trajectory render panel | `src/JitML/Web/Bundle.hs`; panel metadata bound to `RlStream`; `web/src/Panels/Rl.purs` subscribes to `/api/ws/rl`, parses incoming frames with `Generated.Contracts.parseRlAnimationFrame` and `parseRlReplayFrame`, renders reward/action/probability bars plus a prev/next replay scrub summary, exposes daemon-protocol start/stop command controls through `/api/runs/rl-demo/command`, and renders generated queued/running/failed/done `WorkflowStatus`; `web/src/Panels/Stream.js` reports socket failures through typed actions | ✅ Done; frame/control/replay mechanics animate/evaluate trajectories from completed fixed-budget RL artifacts across the model catalog | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.13 / Phase 14 |
| Training metrics panel | `src/JitML/Web/Bundle.hs`; `web/src/Panels/Training.purs` subscribes to `/api/ws/training`, parses incoming frames with `Generated.Contracts.parseTrainingEventFrame`, renders loss/throughput/device/checkpoint/TensorBoard metadata, exposes daemon-protocol start/stop plus kill-as-non-draining-stop command controls through `/api/runs/training-demo/command`, and renders generated queued/running/failed `WorkflowStatus` | ✅ Done; charting shows fixed-budget counters, convergence stats, readiness, and TensorBoard links for each model | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.13 / Phase 14 |
| Tuning frontier panel | `src/JitML/Web/Bundle.hs`; `web/src/Panels/Tune.purs` subscribes to `/api/ws/tune`, parses `Generated.Contracts.TuneTrialFrame` and `TuneSweepDoneFrame`, renders trial heatmap/frontier/promoted-checkpoint summaries, exposes daemon-protocol start/stop controls through `/api/runs/tune-demo/command`, and renders generated queued/running/failed/done `WorkflowStatus` | ✅ Done; frontier controls show fixed trial budgets, promoted-checkpoint eligibility, convergence/objective stats, and TensorBoard/checkpoint links | Sprint 11.4 / Sprint 11.8 / Sprint 11.9 / Sprint 11.11 / Sprint 15.13 / Phase 14 |
| Admin-portal emitter | `src/JitML/Web/AdminPortals.hs` renders `web/src/Generated/AdminPortals.purs` from the `routeAdminPortalLabel` metadata on the `JitML.Routes.Route` record via the local bridge-compatible emitter shape | ✅ Done; drift is gated by `jitml docs check` | Sprint 11.7 |
| Generated admin portals | `web/src/Generated/AdminPortals.purs`; tracked path `web.admin-portals.purescript` in `JitML.Generated.Paths.trackingGeneratedPaths`; `jitml docs check` gates drift | ✅ Done | Sprint 11.7 |
| Shared chrome header | `web/src/Chrome/Header.purs`; pure render of the `jitML` wordmark plus `[home]` link to `#portals`, prepended to every panel's render tree | ✅ Done; live Playwright validates the header on every panel route | Sprint 11.7 |
| SPA panel registry | `web/src/PanelRegistry.purs`; single hand-maintained list of demo panels consumed by `Main.purs` and `Panels.Portals`; panel mounts return Halogen disposers consumed by the hash dispatcher | ✅ Done | Sprint 11.7 |
| Portals home panel | `web/src/Panels/Portals.purs`; default empty-hash landing rendering the header plus a two-column directory composing `PanelRegistry.panels` with `Generated.AdminPortals.adminPortals` | ✅ Done; live Playwright validates the empty-hash home and portal hrefs | Sprint 11.7 |
| Webapp role (`jitml-demo` workload) | `src/JitML/App.hs` `runWebappRole`; `chart/local/jitml-demo` starts `jitml service --config /etc/jitml/BootConfig.dhall`; the role serves the full API route family and exposes `/bundle/main.js` from the `web/dist/Main/bundle.js` bundle baked into `jitml:local` | ✅ Done; live WebSocket proxy and baked bundle were validated through the edge route; no offline DOM fallback remains | Sprint 11.5 / Sprint 11.10 / Sprint 15.13 / Sprint 17.3 |

## CLI Doctrine Components

Components introduced by the doctrine adoption sprints scheduled in
[phase-1-haskell-cli-surface.md](phase-1-haskell-cli-surface.md) and the surfaces
that consume them. Citations name the doctrine sections they implement per
standards rule L.

| Component | Doctrine Section | Status | Owning Sprint |
|-----------|------------------|--------|---------------|
| `CommandSpec` registry as implementation source | Automatically Generated Documentation; Command Topology | ✅ Done | Sprint 1.2 |
| `OptionSpec` record fields (`longName`, `shortName`, `metavar`, `description`, `required`) | Automatically Generated Documentation | ✅ Done | Sprint 1.2 |
| Per-leaf `Example` entries on every `CommandSpec` | Automatically Generated Documentation | ✅ Done | Sprint 1.2 |
| Parser generated from the registry (parser is a renderer, not the source of truth) | Command Topology | ✅ Done | Sprint 1.2 |
| Parser-test category via `execParserPure` | Testing Doctrine → Parser Tests | ✅ Done | Sprint 1.2 |
| `jitml commands --tree` and `jitml commands --json` introspection | Progressive Introspection | ✅ Done | Sprint 1.2 |
| `Subprocess` ADT plus `runStreaming` / `capture` interpreter; pure `renderSubprocess` | Architecture → Subprocesses as Typed Values | ✅ Done | Sprint 1.6 |
| Forbidden subprocess primitives (`callProcess`, `readCreateProcess`, `System.Process` constructors, `typed-process` smart constructors) | Architecture → Subprocesses as Typed Values | ✅ Done | Sprint 1.6 |
| `Plan` / `apply` boundary with `--dry-run` and `--plan-file <path>` | Plan / Apply | ✅ Done | Sprint 1.5 |
| `prerequisiteRegistry` with `nodeId`, `nodeDescription`, remedy hint, transitive closure | Prerequisites as Typed Effects | ✅ Done | Sprint 1.7 |
| `cluster.host-memory` preflight node | Prerequisites as Typed Effects | ✅ Done — `jitml doctor --scope cluster` fails when host `MemTotal` is below the `dhall/cluster/` node cap + reserve so bootstrap refuses to run uncapped on an undersized host; returns pass when `/proc/meminfo` is absent (non-Linux) | Sprint 2.8 |
| Single `Env` record threaded via `ReaderT Env IO` | Application Environment | ✅ Done | Sprint 1.8 |
| Single `AppError` ADT (`PrerequisiteUnmet`, `SubprocessFailed`, `MinIOFailed`, `PulsarFailed`, `HarborFailed`, `KubectlFailed`, `DocsCheckDrift`, `UnknownCommand`, `InvalidConfig`, `DhallTypeError`, `ChartLintFailed`, `RouteRegistryDrift`, `JitCacheMiss`, `JitToolchainDrift`, `CheckpointFormatUnsupported`, `CheckpointWriteConflict`, `InferenceCheckpointMissing`, `InferenceManifestShaMismatch`, `TrainingPrerequisiteUnmet`, `ReconcilerNoop`) | Error Handling | ✅ Done; `TrainingPrerequisiteUnmet` (exit 2) added Sprint 8.10 for fail-closed `jitml train` | Sprint 1.9 (extended Sprints 15.12, 8.10) |
| `renderError :: AppError -> Text` boundary | Error Handling | ✅ Done | Sprint 1.9 |
| Exit code `3` for reconciler no-op-on-match | Error Handling | ✅ Done | Sprint 1.9 |
| HLint rule config and in-repo primitive scan cover terminal output primitives, `exitFailure`, and subprocess constructors outside their approved modules | Error Handling | ✅ Done | Sprint 1.4 |
| `--format json\|table\|plain` (default `table` on TTY else `plain`) | Output Rules | ✅ Done | Sprint 1.9 |
| `--color auto\|always\|never` plus `--no-color` | Output Rules | ✅ Done | Sprint 1.9 |
| `fourmolu.yaml` 13-setting list at repo root | Lint, Format, and Code-Quality Stack → Pinned fourmolu.yaml | ✅ Done | Sprint 1.4 |
| Container-exclusive style-tools bootstrap and Haskell code-quality gate | Lint, Format, and Code-Quality Stack → Tool bootstrap | ✅ Done; `docker/Dockerfile` installs the style GHC/tools, stamps the `jitml:local` code-quality domain, and runs `jitml check-code`; host lint/check-code execution is rejected before linting | Sprint 1.4 |
| `cabal format` temp-file round-trip byte-equality check | Lint, Format, and Code-Quality Stack | ✅ Done | Sprint 1.4 |
| `forbiddenPathRegistry` (`.github/workflows/`, `.husky/`, `.githooks/`, `.pre-commit-config.yaml`, root `Makefile`/`justfile`/`Taskfile.yml`) | Lint, Format, and Code-Quality Stack → Forbidden Surfaces | ✅ Done | Sprint 1.4 |
| `GeneratedSectionRule` registry for marker-delimited generated regions | Generated Artifacts → The generated-section registry | ✅ Done | Sprint 1.3 |
| `trackingGeneratedPaths` registry for active fully-generated files (`documents/cli/commands.md`, `share/man/man1/jitml.1`, shell completions, PureScript contracts, chart HTTPRoutes, Grafana dashboards, Prometheus scrape config) | Generated Artifacts → Two categories of generation | ✅ Done | Sprint 1.3 |
| `futureTrackingGeneratedPathPatterns` registry for later generated artefacts (`share/man/man1/jitml-*.1`) | Generated Artifacts → Two categories of generation | ✅ Done | Sprint 1.3 |
| GADT-indexed `TrainingLifecycle`, `RLRunLifecycle`, and `TuneSweepLifecycle` with singleton witnesses in `src/JitML/RL/Framework.hs` (`RLRunLifecycle` indexes the `RLRunPhase` data kind: `RLCollect`, `RLComputeAdvantages`, `RLOptimise`, `RLEvaluate`, `RLCheckpoint`); `JitML.Training.Budget` adds the shared fixed-budget `CompletedTraining` witness consumed by checkpoint eligibility | GADT-Indexed State Machines | ✅ Done; lifecycle and completion witness types are threaded through SL/RL/tune completion payloads and observed by the live all-model matrix | Sprints 8.4, 8.6, 8.7, 8.14, 9.7 |
| Capability classes (`HasMinIO`, `HasPulsar`, `HasHarbor`, `HasKubectl`) — full doctrine-required methods checked in | Capability Classes and Service Errors | ✅ Done; `HasMinIO`, one-shot and held-open-worker `HasPulsar` surfaces, `HasHarbor`, and `HasKubectl` subprocess instances are live-validated, `DaemonServiceClient` exposes all four class instances from BootConfig-derived settings, startup acquisition calls the typed `HasPulsar.pulsarSubscribe` boundary in local tests with zero-queue WebSocket subscribe-probe rendering, the real Linux CPU daemon pod live-validates acquired MinIO list, Harbor list, and in-cluster `kubectl get pods` probes, 2026-05-21 live `--consume-once` dispatches Training/RL/Tune/Inference messages plus `WriteCheckpointBlob`, `PromoteWorkloadImage`, and `RunInference` effects from the service pod before ack, the normal serve path starts held-open consumers through the same `DaemonServiceClient`, live duplicate-payload dedup and dispatch-failure negative-ack redelivery are validated, and `JitML.Service.Workload` locally parses and dispatches MinIO write/CAS, Harbor promote, kubectl apply/status/delete, and RunInference payloads | Sprints 4.4, 5.4, 5.5 |
| `RetryPolicy` typed value with named strategies | Retry Policy as First-Class Values | ✅ Done | Sprint 5.4 |
| At-least-once Pulsar consumer with protobuf-message-hash deduplication | At-Least-Once Event Processing | ✅ Done; local dispatcher, fully-qualified topic routing, BootConfig-derived subscription planning, startup acquisition status, bounded acquired-subscription daemon batch, normal-service held-open workers sharing one process-lifetime router, zero-queue WebSocket subscribe-probe and held-open worker command rendering, LiveConfig-derived dedup cache size/TTL, TTL-boundary expiry, dispatch-failure seek-without-dedup-poisoning, synthetic subscribe/consume/ack/dedup tests, 2026-05-21 live service-pod bounded consume/dispatch/ack validation, 2026-05-21 live normal-service held-open `RunInference` → `InferenceResult` validation without `--consume-once`, live duplicate-payload dedup, and live dispatch-failure negative-ack redelivery are checked in | Sprint 5.5 |
| Long-running daemon shape (`BootConfig` / `LiveConfig`, SIGHUP, `/healthz`, `/readyz`, `/metrics`, structured JSON stderr logging) | Long-Running Daemons in the Same Binary | ✅ Done; local lifecycle, endpoint, logging, signal, held-open worker surfaces, and HA compute cardinality rendering are validated on the code surface. | Sprints 5.2, 5.3, 5.6, 5.16 |
| Implemented reconciler discipline (`jitml docs generate`, `jitml lint --write`, `jitml bootstrap`, `jitml cluster up`, `--dry-run` / `--plan-file` plan rendering, no-op exit code `3`) | Reconcilers: Idempotent Mutation as a Single Command; Plan / Apply | ✅ Done | Sprints 1.3, 1.4, 1.5, 1.9, 2.1, 3.5 |
| Mutating reconciler effects (`jitml bootstrap` live Kind/Helm apply, `jitml internal gc` live MinIO deletion) | Reconcilers: Idempotent Mutation as a Single Command | ✅ Done for current HA bootstrap materialization; live HA apply revalidation passed in Phases `15`/`16`. | Sprints 3.5, 3.6, 10.3, 15.7 |
| Cabal-manifest toolchain pin (`tested-with: ghc ==9.12.4` in `jitml.cabal`, `with-compiler: ghc-9.12.4` in `cabal.project`; CUDA/NVCC/cuDNN, Node, Playwright, ALE, Metal bridge/runtime, Kind node image, LLVM, and oneDNN ownership documented at their package/probe surfaces) with no `allow-newer` override and no dependency helper pins | Toolchain pinning | ✅ Done; Sprint `1.11` keeps one GHC `9.12.4` baseline for the project and style tools, while codegen toolchains are documented/probed according to the reality captured in README toolchain pinning. | Sprint 1.1 / Sprint 1.11 |
| Library-first layout audit (thin `app/Main.hs`, role logic in `src/JitML/`) | Project Structure | ✅ Done | Sprint 1.1 / Sprint 11.10 |
| Durable CLI documentation artefacts (`documents/cli/commands.md`, `share/man/man1/jitml*.1`, `share/completion/{bash,zsh,fish}/`) | Automatically Generated Documentation | ✅ Done | Sprint 1.3 |
| Standardized library set audit in `jitml.cabal` (`optparse-applicative`, `text`, `bytestring`, `aeson`, `prettyprinter*`, `ansi-terminal`, `path`, `path-io`, `typed-process`, `safe-exceptions`, `dhall`, `tasty*`, `temporary`) | Overview → standardized stack | ✅ Done | Sprint 1.1 |

## Test Stanzas

Per doctrine `Test Organization`, each tier is a separate Cabal `test-suite` with
`type: exitcode-stdio-1.0` and `tasty` as the in-stanza runner. A single `tasty`
tree spanning all tiers is forbidden. The eight report-card stanza declarations
exist today, and each non-style phase stanza now has a dedicated local
deterministic body.

| Stanza | Current body | Target expansion | Status | Owning Sprint |
|--------|--------------|------------------|--------|---------------|
| `jitml-unit` | `test/unit/Main.hs` covers current parser, docs, prerequisite, env, app-error, plan, subprocess, bootstrap-script, cache, runtime-source, daemon-surface, checkpoint, frontend, Grafana fixture, and catalog helpers | Final Pure Logic, Parser, Property, and Snapshot (pure-renderer output only) coverage across engine invariants, richer transcript codecs, RNG mixers, generated Grafana fixture breadth, and richer RL/tuning/checkpoint codecs | ✅ Done | Sprint 12.1 |
| `jitml-integration` | `test/integration/Main.hs` covers typed subprocess execution, renderer suites, route-table snapshots, real-binary spawn matrix, filesystem-backed and live `HasMinIO` checkpoint snapshot write / inference / resume coverage, local Linux CPU checkpoint inference through a generated oneDNN FFI kernel, decoded `.jmw1` weights passed into the weighted local inference runner, routed MinIO/Pulsar/Harbor subprocess command rendering, BootConfig-derived daemon client settings, Dhall numerics decode, linkable oneDNN probing, live daemon dispatch effects, live checkpoint/GC/tune/AlphaZero/SL/RL/inference cases, typed live-rollout command shapes, and the Sprint 12.11 `Live` `WorkflowMatrix` runner that filters the matrix to the current substrate and fails closed without `cluster-publication.json` | Sprint `12.15` expands the live matrix from workflow/category cells to each SL row, RL algorithm, AlphaZero game, and tuning promotion with trained-artifact eligibility and negative inference checks | ✅ Done; live matrix coverage is sufficient for `linux-cpu` all-model fixed-budget closure and accelerator reruns are owned by Phases `15`/`16` | Sprint 12.2 / 12.11 / 12.13 / Sprint 12.15 / Phase 15 / Phase 14 |
| `jitml-sl-canonicals` | `test/sl-canonicals/Main.hs` exercises canonical `(dataset, model)` rows as property tests over the typed `TrainingLifecycle`, dataset `HasMinIO` fetch/verification, `TrainingCommand` text/proto3 byte round-trips, supervised experiment Dhall row resolution, MNIST/Fashion-MNIST real-artifact SHA coverage, CIFAR-10/CIFAR-100 real archive SHA coverage plus archive-backed binary-batch parser coverage, California Housing real archive SHA coverage plus archive-backed regression-row parser and device-backed MSE convergence coverage, Tiny ImageNet real archive SHA coverage plus Zip64/JPEG tensor materialization coverage, the live MNIST convergence assertion with `RunConfig` budget consumption through the same `JitML.SL.Architecture` + selected `MlpDevice` runtime as `jitml train`, Sprint `8.12` all-row substrate-backed train-step coverage through `JitML.SL.Architecture`, and a focused live linux-cpu all-row staged-byte train/eval smoke that fetches every canonical artifact from MinIO; no per-substrate `.txt` curve fixtures are committed per [../README.md → Snapshot targets → Numerical-fixture prohibition](../README.md#snapshot-targets) | Every canonical row completes its fixed training budget, emits convergence stats, writes an eligible checkpoint, reloads/evaluates/infers, and rejects partial artifacts in the `linux-cpu` baseline | ✅ Done | Sprint 12.3 / Sprint 8.12 / Sprint 8.14 / Sprint 12.15 / Sprint 15.4 / Phase 13 |
| `jitml-rl-canonicals` | `test/rl-canonicals/Main.hs` covers algorithm metadata, run-to-run trajectory determinism, rule-conformance property tests for canonical AlphaZero games, `RlCommand` text/proto3 byte round-trips, and live/statistical RL surfaces through Phase `15`; no trajectory or AlphaZero-transcript files are committed per [../README.md → Snapshot targets → Numerical-fixture prohibition](../README.md#snapshot-targets) | Every RL algorithm and AlphaZero game trains/evaluates/rolls out from fixed-budget eligible policy artifacts and rejects incomplete checkpoints in the `linux-cpu` baseline | ✅ Done | Sprint 12.4 / Sprint 9.12 / Sprint 9.14 / Sprint 12.15 / Phase 15 / Phase 13 |
| `jitml-hyperparameter` | `test/hyperparameter/Main.hs` covers the full target sampler catalog, scheduler / pruner axes including TPE, sampler-state-purity property tests, TPE worked-example Dhall decode, sampler-label parsing, report-card knob consumption, checkpointable trained weights for measured trial objectives, `TuneCommand` text/proto3 byte round-trips, and live trial persist/replay through Phase `15`; no per-sampler trial-value fixtures are committed per [../README.md → Snapshot targets → Numerical-fixture prohibition](../README.md#snapshot-targets) | Resume-from-partial-sweep equality, best-checkpoint promotion, fixed trial-budget completion, eligible promoted checkpoints, and browser-visible sweep controls/frontiers | ✅ Done; tuning coverage includes completion/eligibility metadata and promoted-checkpoint trained-artifact proof | Sprint 12.5 / Sprint 9.12 / Sprint 10.10 / Sprint 12.15 / Sprint 15.10 / Phase 14 |
| `jitml-backends` | `test/backends/Main.hs` covers generated Linux CPU oneDNN identity/reorder, reduction, matmul, convolution, normalization, attention, and embedding kernel compile/load/run (within-substrate run-to-run bit-identity), loaded family/output-count symbol checks, local Linux CPU `HasEngine` dispatch over a generated family kernel, and the analogous within-substrate linux-cuda and apple-silicon weighted-kernel determinism cases (the two substrate-agnostic cases — engine determinism flags, backend-independent manifest reads — relocated to `jitml-unit` in Sprint 12.10). Each substrate's cases run for real in that substrate's lane through `jitml test jitml-backends --<substrate>`, which synthesizes `-p <substrate>` and adds `-fcuda` on `linux-cuda`; there are no skipped tests and no cross-substrate comparison | Per-substrate run-to-run bit-identity; no cross-substrate cohort and no per-tensor stored fixtures per [../README.md → Snapshot targets](../README.md#snapshot-targets) | ✅ Done (Sprint 12.10 removed the `CrossSubstrate` drift group + skip guards, landed 2026-06-09; all three lanes re-validated for real — `apple-silicon` 4 / 4, `linux-cpu` 10 / 10, and `linux-cuda` 19 / 19 on the RTX 5090 (Sprint 15.16), no skip-sentinels) | Sprint 12.6 / 12.10 |
| `jitml-daemon-lifecycle` | `test/daemon-lifecycle/Main.hs` covers lifecycle / retry / signal-control tests, inference request/result protobuf byte round-trips, fully-qualified Pulsar topic routing, BootConfig-derived subscription planning, synthetic subscribe/consume/ack/dedup behavior, plus one-shot daemon HTTP `/healthz` | Real Pulsar idempotency assertion against a live consumer | ✅ Done | Sprint 12.7 |
| `jitml-e2e` | `test/e2e/Main.hs` plus `src/JitML/Test/LivePlan.hs` covers route/bucket/chart-values/publication/contract/demo HTTP including generated stream routes/report/no leaked `jitml-e2e-*` clusters when `kind` is present and the active Docker context answers `docker info`, the typed `helm dependency build` → `jitml bootstrap` → Playwright → `jitml cluster down` live plan, and the Sprint 12.11 structural assertion that `JitML.Test.WorkflowMatrix` covers every reopened workflow on every substrate | Explicit live e2e path brings up an ephemeral Kind stack via `jitml bootstrap`, runs Playwright against real Envoy, starts each supported model workflow, verifies training/checkpoint/inference interactions, observes RL animations, drives adversarial games, replays saved transcripts, exercises tuning controls, proves infer-before-complete rejection, and tears down via `jitml cluster down` | ✅ Done; route/teardown, structural coverage, all-model product matrix, and trained-artifact UI state are validated for `linux-cpu` | Sprint 12.8 / 12.11 / 12.13 / Sprint 12.15 / Sprint 14.4 / 15.14 / Phase 14 |
## Test Categories Mapping (Doctrine → Stanza)

| Doctrine Test Category | Owning Stanza |
|------------------------|---------------|
| Pure Logic | `jitml-unit` |
| Parser | `jitml-unit` |
| Property | `jitml-unit` |
| Snapshot (pure-renderer output only) | `jitml-unit` |
| Integration | `jitml-integration`, `jitml-sl-canonicals`, `jitml-rl-canonicals`, `jitml-hyperparameter`, `jitml-backends` (the four canonicals/HPO/backend rows are project-specific Integration per doctrine §Test Organization → project-specific stanzas) |
| Daemon Lifecycle | `jitml-daemon-lifecycle` |
| Ephemeral-Cluster Infrastructure | `jitml-e2e` |

## POC Report-Card Knobs

Pinned in `cabal.project` for reproducibility across hosts; see
[phase-12-test-stanzas-and-cross-cluster.md](phase-12-test-stanzas-and-cross-cluster.md).

| Knob | Value | Purpose |
|------|-------|---------|
| `sl_epochs` | `5` | Canonical SL training epoch count for the convergence-check budget |
| `sl_batch` | `64` | Canonical SL minibatch size for the convergence-check budget |
| `rl_steps` | `100_000` | Canonical RL training step budget for the convergence-check budget |
| `rl_eval_episodes` | `25` | Per-eval episode count for the RL Evaluator |
| `az_games` | `200` | Self-play game count per AlphaZero generation in the convergence-check suite |
| `az_sims` | `400` | MCTS simulation budget per move in the AlphaZero convergence-check suite |
| `tune_trials` | `64` | Trial count for the report-card tuning workload |
| `tune_budget_per_trial` | `1_000` | Per-trial step budget for the report-card tuning workload |
| `xcluster_kind_nodes` | `2` | Future cross-cluster e2e stack node-count knob; current per-substrate local Kind configs use the HA worker count from `dhall/cluster/resources.dhall` |

## Toolchain

| Component | Pinned Version | Purpose | Status | Owning Sprint |
|-----------|----------------|---------|--------|---------------|
| GHC | `9.12.4` | Haskell compiler for the CLI binary, the daemon, and every library module | ✅ Done | Sprint 1.1 |
| Cabal | `3.16.1.0` | Haskell build tool; per-stanza `type: exitcode-stdio-1.0` | ✅ Done | Sprint 1.1 |
| Haskell dependency helper pins | _removed_ | The project now solves from plain Hackage under GHC `9.12.4` / `base-4.21`; `cabal.project` has no `allow-newer`, no `source-repository-package` entries, and no local dependency packages | ✅ Removed | Sprint 1.11 / Sprint 17.3 |
| LLVM / Clang | Ubuntu 24.04 `llvm` + `clang` packages in `docker/Dockerfile`; host LLVM/Clang come from the host toolchain used to build `jitml`. On Apple stage-0 builds, `bootstrap/_lib.sh` accepts PATH `opt`/`llc` in GHC 9.12.4's supported `[13,20)` range or prepends an installed Homebrew `llvm@19` ... `llvm@13` keg before invoking Cabal. | Shared by GHC `-fllvm` and JIT codegen; `clang` is the assembler GHC invokes for the LLVM backend | ✅ Done | Sprint 1.1 |
| NVCC | pinned in `cabal.project` (`--use_fast_math=false`, baseline `sm_70`) | CUDA kernel codegen for `linux-cuda` | ✅ Done | Sprint 7.4 |
| ALE | upstream `v0.12.0` commit `94c24368664b8539c53857522e50652ddcc44b20` library/runtime prerequisite in `docker/Dockerfile` | Optional Arcade Learning Environment runtime for `atari-subset`; Haskell loads `JitML.RL.ALE` only when an explicit ROM path is supplied, and any native adapter must be generated into the build/cache tree or supplied externally via `JITML_ALE_SHIM_PATH`. Default examples and required canonical tests use copyright-free environments. | ✅ Done for no-ROM fail-closed policy and static C++ shim removal; real ALE smoke is optional/manual and requires generated/external adapter support | Sprint 8.8 |
| Metal (Apple) | host OS Metal framework + fixed jitML Metal bridge; bridge ABI and Metal runtime policy are cache-key inputs | `apple-silicon` Metal kernel codegen: Haskell-rendered MSL source metadata at `<hash>.metal.json` + fixed bridge runtime `MTLDevice.makeLibrary(source:options:)` shader compilation. No core Tart, SwiftPM, full Xcode, offline `metal`, or keychain dependency. | ✅ Done; fixed bridge installed/probed and live Apple backend/e2e/workflow lanes passed on 2026-06-12 | Sprint 7.11 / Sprint 16.9 |
| oneDNN | `libdnnl-dev` installed in `docker/Dockerfile`; AVX2 baseline, AVX-512 detected at JIT time | CPU kernel codegen and local FFI execution for `linux-cpu` | ✅ Done | Sprint 7.3 |
| `kindest/node` | pinned in `./kind/cluster-<substrate>.yaml`; mirrored as a comment in `cabal.project` | Kind node image; the comment-mirror is a single-source-of-toolchain-truth record (`jitml lint chart` rejects drift) | ✅ Done | Sprint 3.1 |
| `tart` | legacy Apple build-VM tool | No longer part of the supported core Apple Metal JIT path. The command surface, prerequisite/bootstrap cleanup, daemon acquire/config, and generated Swift/Tart cache-miss code path are removed; only dated historical validation notes mention it. | ✅ Retired from the supported path | Sprint 7.11 |
| `kind` | Haskell prerequisite DAG | Kubernetes-in-Docker | ✅ Done | Sprint 2.2 |
| `kubectl` | Haskell prerequisite DAG | k8s API client invoked through the typed `Subprocess` boundary | ✅ Done | Sprint 2.2 |
| `helm` | Haskell prerequisite DAG | Helm CLI invoked through the typed `Subprocess` boundary | ✅ Done | Sprint 2.2 |
| `docker` | stage-0 Linux gate plus Haskell prerequisite DAG | Container runtime; the only host runtime touched on Linux | ✅ Done | Sprint 2.2 |
| Node.js | Haskell prerequisite DAG | Required by the PureScript toolchain (`spago`, `purescript`) | ✅ Done | Sprint 2.2 |
| Poetry | Haskell prerequisite DAG | Required for ancillary Python tooling (none on the supported runtime path; only present for codegen support tools) | ✅ Done | Sprint 2.2 |
| Haskell style tools | built with GHC `9.12.4` | Container-exclusive lint stack built with the same pinned compiler as the project; host lint/check-code execution is unsupported | ✅ Done | Sprint 1.4 / Sprint 1.11 |
| PureScript contract generator | local renderer in `src/JitML/Web/Contracts.hs` | Bridge-compatible endpoint renderer for `web/src/Generated/Contracts.purs`; no external `purescript-bridge` package is required for the current implementation | ✅ Done | Sprint 11.2 |
| Target platforms | `arm64` macOS (Apple Silicon), `amd64` Linux, optional `arm64` Linux | Three substrates × supported host arches | ✅ Done | Phases 3, 7 |

## State Locations

| State Class | Authority | Durable Home | Notes |
|-------------|-----------|--------------|-------|
| Build artefacts | `cabal` (Apple) or `docker compose run` (Linux) | `./.build/` (gitignored, dockerignored) | The only host folder holding compiled artefacts |
| Generated JIT compiler inputs | `jitml` Haskell runtime source renderers | `./.build/jit-src/<substrate>/<hash>/` | CUDA, oneDNN C++, and Apple MSL inputs are generated on demand; optional Swift inputs are separate non-core capability work. The local Linux CPU oneDNN primitive paths compile and load from this tree, then validate exported family and output-count metadata; CUDA inputs expose a host-callable wrapper and guarded loader boundary. |
| JIT cache | `jitml service` and `jitml build` | Linux: `./.build/jit/<substrate>/<hash>.so`; Apple target: `./.build/jit/apple-silicon/<hash>.metal.json` | Survives `purge`; only `purge --full` removes it; local Linux CPU fingerprints include `artifact-abi=<os>-<arch>` so host and container artifacts do not collide; Apple cache keys include bridge ABI, Metal runtime policy, MSL source, launch metadata, determinism options, and tuning choice |
| Apple fixed bridge | `jitml service` (host-native instance) and `jitml internal install-metal-bridge` | fixed bridge under `./.build/host/apple-silicon/`; per-kernel Apple source metadata under `./.build/jit/apple-silicon/<hash>.metal.json` | The fixed bridge is process-stable and not generated per kernel; per-model generated-dylib symlinks are deleted |
| Kubeconfig | `jitml bootstrap --<substrate>` | `./.build/jitml.kubeconfig` | The CLI never touches `~/.kube/config` |
| Generated Dhall and runtime metadata | `jitml bootstrap --<substrate>` | `./.build/conf/`, `./.build/conf/cluster/Resources.dhall`, `./.build/runtime/cluster-publication.json`, `./.build/kind/<substrate>/` | Host Dhall exists only on Apple; Linux uses only the cluster ConfigMap Dhall; `Resources.dhall` is the materialized cluster resource profile (node cap + per-pod budgets) decoded from the `dhall/cluster/` source |
| Kind hostPath PV state | per-StatefulSet PVs | `./.data/<namespace>/<StatefulSet>/pv_<replica-int>/` | `.data` is strictly manual PV bind mounts; lint-enforced by `jitml lint files` |
| Checkpoint store | `jitml service` (training path); local interpreter and `HasMinIO` writer in `JitML.Checkpoint.Store` | MinIO `jitml-checkpoints/<experiment-hash>/{blobs,manifests,pointers}`; local tests use a filesystem object root with the same key layout | Concurrency: write-once + If-Match CAS on pointers; local tests exercise write-once objects, manifest writes, manifest discovery, latest pointer CAS, `HasMinIO` snapshot writes, GC planning, and inference from latest. ✅ Done for Sprint `10.11`: local object-key validation returns typed errors before path construction, and unsafe experiment-hash-derived prefixes render as `InvalidConfig` at the CLI boundary instead of `error`. |
| Trial store | `jitml tune` | MinIO `jitml-trials/<sha256(resolved-dhall \|\| trial-seed)>/` | Trial transcripts content-addressed |
| TensorBoard events | `jitml service` writers | MinIO `jitml-tensorboard/<experiment-hash>/shards/*.tfevents` plus checkpoint sidecars | Stateless TB pod reads from MinIO |
| RL transcripts | `jitml rl train` / `jitml rl rollout` | MinIO `jitml-transcripts/` | Analog of MCTS's `.mcts-cache/transcripts/` |
| Plan suite | repository worktree | `DEVELOPMENT_PLAN/` | This document set |
| Doctrine | repository worktree | `README.md` (root) | Authoritative CLI doctrine |
| Governed engineering docs | repository worktree | `documents/engineering/` | Project-specific elaborations of the doctrine and project-owned content |
| Generated-section registry and docs metadata lint | code | `src/JitML/Generated/Registry.hs`, `src/JitML/Docs/Check.hs` | Authoritative for `jitml docs check` and `jitml docs generate`. ✅ Done for Sprint `0.3`: the checker enforces the governed-document metadata contract (`Status`, `Supersedes`, `Referenced by`, `Generated sections`, and `Purpose`) and reconciles generated-section metadata with physical markers and the registry. |
| Tracking-generated-paths registry | code | `src/JitML/Generated/Paths.hs` | Authoritative for `jitml lint files` drift detection |

## Artefact Locations

This table is the local artefact inventory for the implemented plan surfaces.
The current concrete worktree is summarized in
[00-overview.md → Current Baseline](00-overview.md#current-baseline).

| Type | Location | Purpose |
|------|----------|---------|
| Haskell application entrypoint | `app/Main.hs` | Thin shim into `App.main` per the library-first layout; Webapp serving is selected by `BootConfig.activeRole` |
| Haskell source modules | `src/JitML/` | CLI, cluster, daemon, runtime, SL/RL/AlphaZero/tuning, proto, engines, codegen, numerics, storage, inference, web, observability, generated-artifact tracking, checkpointing, `Test.{LivePlan,Report}`, bootstrap live rollout, and typed `Subprocess` support |
| Cabal package definition | `jitml.cabal` | Build, test, and dependency definition with `tested-with: ghc ==9.12.4`; declares the `jitml` executable and the eight test-suite stanzas |
| Cabal project definition | `cabal.project` | Repository-wide Cabal package-set definition with `with-compiler: ghc-9.12.4`, the codegen-toolchain pins, and the report-card knobs |
| Formatter config | `fourmolu.yaml` | Pinned 13 doctrine-mandated settings at repo root |
| Per-substrate Kind configs | `./kind/cluster-{apple-silicon,linux-cpu,linux-cuda}.yaml` rendered by `JitML.Cluster.Kind` | Checked-in configs use one `control-plane` plus three workers for every substrate; every node bind-mounts `./.build/` and `./.data/` via `extraMounts`; Linux CUDA wires worker containerd `nvidia` runtime handlers and read-only host driver-root mounts for `RuntimeClass/nvidia` |
| Bootstrap scripts | `./bootstrap/{apple-silicon,linux-cpu,linux-cuda}.sh` | Stage-0 idempotent reconcilers |
| Docker assets | `docker/Dockerfile`, `compose.yaml` | One Dockerfile producing one image (`jitml:local`) with the `jitml` executable and the style-tool/code-quality gate; the live rollout retags it as `jitml-demo:local` for the Webapp workload. Two host-networked compose service wrappers run over that image: headless `jitml` for bootstrap/code-quality/non-GPU command runs, and GPU-enabled `jitml-cuda` for direct live CUDA validation |
| Helm umbrella chart | `chart/Chart.yaml`, `chart/values.yaml`, `chart/values/`, `chart/templates/`, `chart/local/` | Subchart deps for Harbor, Pulsar, MinIO, Postgres, Envoy Gateway, Prometheus; checked-in local charts for TensorBoard, `jitml-service`, and `jitml-demo`; typed per-release values for direct dependency installs; templates for GatewayClass / Gateway / HTTPRoutes / EnvoyProxy / manual PVs / Deployments / RuntimeClass / dashboards; umbrella values live in `chart/values.yaml` unless a typed Helm subprocess passes a file from `chart/values/` |
| JIT source generation | `src/JitML/Codegen/{RuntimeSource,Cuda,MlpCuda,OneDnn,Metal,SourceFile}.hs`, `src/JitML/Engines/{CudaLocal,HasEngine,Local,MetalBridge}.hs`, `./.build/jit-src/<substrate>/<hash>/` | Haskell-generated compiler inputs for CUDA, oneDNN C++, and Apple MSL; target Apple cache writes `.metal.json` source metadata consumed by the fixed bridge. Local Linux CPU oneDNN primitive compile/load/run plus family/output-count symbol validation, local Linux CPU `HasEngine` dispatch, guarded CUDA local loader / `LocalCudaEngine` preflight dispatch, and host/container artifact ABI separation in the Linux CPU fingerprint remain unchanged |
| Protobuf contracts | `proto/tensorboard/event.proto`, `proto/jitml/{training,rl,tune,inference}.proto` | TensorBoard scalar Event schema plus jitML command/event envelopes |
| PureScript frontend | `web/package.json`, `web/spago.yaml`, `web/src/`, `web/test/`, `playwright/` | PureScript shell, generated contract file, `spec-node` smoke tests, Halogen panels, live-only Playwright scaffold, and `jitml-demo` route surface |
| Experiments | `experiments/` | Canonical experiment Dhall files (the "configuration is code" surface) |
| Cluster + run config Dhall | `dhall/cluster/` (resource budgets: kind-node cap + per-pod limits/replicas), `dhall/run/` (typed `RunConfig` worker parameters) | Typed Dhall sources decoded at bootstrap / worker dispatch; replace ad-hoc resource defaults and the `JITML_*` run-parameter env IPC |
| Tests | `test/` | Per-stanza test trees (`test/unit/`, `test/integration/`, `test/sl-canonicals/`, `test/rl-canonicals/`, `test/hyperparameter/`, `test/backends/`, `test/daemon-lifecycle/`, `test/e2e/`) and pure-renderer snapshot fixtures under `test/snapshots/{cache,cli,cluster,observability,prerequisite}/`. No `test/golden/` directory — numerical fixtures (per-substrate training curves, RL trajectories, AlphaZero transcripts, per-tensor backends deltas, sampler trial values, etc.) are forbidden per [../README.md → Snapshot targets → Numerical-fixture prohibition](../README.md#snapshot-targets). |
| Development plan | `DEVELOPMENT_PLAN/` | This plan suite |
| Doctrine | `README.md` | Authoritative CLI doctrine at repo root |
| Project README | `README.md` | Project intent, command surface, doctrine scope, build instructions |
| Agent guardrails | `AGENTS.md`, `CLAUDE.md` | Git-command restrictions and doctrine pointers for LLM agents |
| Generated CLI artefacts | `documents/cli/commands.md`, `share/man/man1/jitml*.1`, `share/completion/{bash,zsh,fish}/` | Fully-generated; tracked by `trackingGeneratedPaths`; hand edits fail `jitml lint files` |

## Related Documents

- [README.md](README.md)
- [00-overview.md](00-overview.md)
- [development_plan_standards.md](development_plan_standards.md)
- [legacy-tracking-for-deletion.md](legacy-tracking-for-deletion.md)
- [../README.md](../README.md)
