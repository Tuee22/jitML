apiVersion: v1
kind: Service
metadata:
  name: jitml-service
  namespace: platform
spec:
  type: ClusterIP
  selector:
    app: jitml-coordinator
    jitml.role: coordinator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
