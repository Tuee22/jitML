apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: jitml-edge
  namespace: platform
spec:
  gatewayClassName: jitml-gateway
  listeners:
    - name: http
      protocol: HTTP
      port: 9092
      allowedRoutes:
        namespaces:
          from: All
