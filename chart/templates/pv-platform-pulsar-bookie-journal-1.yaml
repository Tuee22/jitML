apiVersion: v1
kind: PersistentVolume
metadata:
  name: platform-pulsar-bookie-journal-pv-1
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: jitml-manual
  local:
    path: /jitml/.data/platform/pulsar-bookie-journal/pv_1/
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: Exists
  claimRef:
    namespace: platform
    name: pulsar-bookie-journal-pulsar-bookie-1
