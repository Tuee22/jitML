services:
  jitml:
    image: jitml:local
    build:
      context: .
      dockerfile: docker/Dockerfile
    network_mode: host
    working_dir: ${PWD}
    volumes:
      - .:${PWD}
      - /var/run/docker.sock:/var/run/docker.sock
