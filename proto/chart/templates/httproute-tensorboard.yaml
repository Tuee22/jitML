apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tensorboard
  namespace: platform
  labels:
    app.kubernetes.io/part-of: jitml
spec:
  parentRefs:
    - name: jitml-edge
      namespace: platform
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /tensorboard
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: tensorboard
          port: 80
