apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-daemon-health
  namespace: platform
  labels:
    grafana_dashboard: "1"
data:
  daemon-health.json: |
    {
      "title": "daemon-health",
      "panels": [{"type":"timeseries","targets":[{"expr":"jitml_daemon_ready"}]}]
    }
